# Satoru — мастер-план арта и анимации + что ещё большого не сделано (2026-06-29)

> Консолидация всех решений: `STYLE-DECISION.md` (стиль+анимация), `ART-PIPELINE.md` (производство), `PET-PIPELINE.md` (контракт), `COMPETITORS-2.md` (Логово), `PETS-EQUIPMENT-PLAN.md`. Источник правды для арт-трека и хэндоффа.

---

## ЧАСТЬ 1 — Что ещё большого НЕ реализовано

### 🎨 АРТ-МИР (самое крупное, недоделано на 90%)
Код-движки готовы/доказаны (paper-doll, слоты экипировки, 5 видов питомцев, Логово v1, сцена). **Бутылочное горлышко — само производство арта в зафиксированном стиле.** Не сделано:
- Реальный **chibi-аватар** (сейчас «PowerPoint»-редактор-заглушка).
- Реальные **chibi-части питомцев** (сейчас мои черновые SVG-болванки).
- **Боссы/мобы** (визуальный «против чего боролись» — вообще нет арта).
- **Анимация** через cutout-риг/спрайты — кроме моего код-idle.
- Style-модель (Scenario на работах Виолы) — не обучена.

### 🏠 ЛОГОВО (флагман по COMPETITORS-2) — есть v1, недоделано
- **Мебель/декор комнаты** (toggle, кастомизация пространства).
- **Гир видимо на теле** аватара (SelfQuest) — сейчас гир = абстрактные %.
- **Поза/аура аватара по энергии/форме** (устал/бодр).
- **Эмбиент-аудио** (дождь/огонь/лофи — Spirit City; нужны royalty-free ассеты).
- **Генерация вариантов комнаты** (разные обои/пол/окно/декор).

### 💙 Забота / Режим восстановления-траура
Тёплые тексты, Тень заботится в трудные периоды. База-маскот есть, режим — нет. Чувствительное, с Альбертом.

### 🧠 Интервальное всплывание (Gizmo)
Ресёрфейс целей/рефлексий/записей дневника в научных интервалах, сшить с чек-инами Тени. Нет.

### 💳 Монетизация и виральность
- **Stripe/Paddle** перед реальными платежами (фундамент Pro/триал есть).
- **Share-to-unlock-premium** (виральный рост, Skillion). Нет.

### 🔌 Интеграции (в основном внешне-заблокированы)
- Strava (заблок: подписка API), Garmin Body Battery (партнёрское одобрение), Apple Health (нужен натив), Whisper-транскрипция голос/видео.
- **MCP-коннектор для Claude Desktop** (ИИ читает/планирует данные).

### ⚖️ Балансные слои (§6 GUIDE-BALANCE, запаркованы)
Деградация/decay формы и мастерства; типы узлов (навык/поддержание/проект); режимы-цели (🔼/⚖️/⏸️); честный ИИ-импорт уровня; братство/мэтчинг по навыку.

### 🎵 Аудио/музыка
Эпичная фоновая музыка (#23b) + рейд-момент. Не код — нужны лицензированные ассеты.

### 🔴 Блокеры запуска (если релиз — важнее новых фич)
Креш Календаря/Статистики на данных Виолы; персист+бэкап фидбека на Railway; кросс-девайс синк энергии.

### 🟢 Недоделанные хвосты текущих фич
Питомцы Фазы 3–4 (chibi-аватар + гардероб-UI); полировка/тюнинг.

---

## ЧАСТЬ 2 — Полная инструкция: как анимируем, к чему пришли, что делать дальше

### Вывод (зафиксировано), к которому пришли по кастомному персонажу и анимации

1. **Стиль** = чистое мягкое аниме / chibi («chibi-исследователь»): крупные читаемые силуэты, мягкие тени/градиенты, ограниченная палитра. **НЕ** пиксель (как все конкуренты), **НЕ** ультра-детальный концепт (медленно + плохо двигается частями), **НЕ** голый лоу-поли. Комната — чуть рисованнее, но В ТОЙ ЖЕ палитре.

2. **Анимация = cutout / paper-doll, оживляемая В КОДЕ.** Художник (или AI) даёт персонажа/животное **разобранным на части по слоям** (голова/торс/руки/ноги + причёска/одежда/гир отдельно). **Мы один раз** пишем **шаблоны анимаций** (idle-дыхание / отжимания / меч / игра / happy / sleep) и применяем к ЛЮБОМУ собранному персонажу/питомцу. Это и есть «пресет-анимации на любого аватара». **Доказано пилотом** — мой paper-doll движок в апке уже двигает части кодом (idle/уши/хвост/моргание) + надевает экипировку в слоты.

3. **Это ТРИ задачи, не одна** (важно — разделяем узкое место):
   - **(1) Генерация** базы (рисование) · **(2) Риггинг** (разбор на части/скелет) · **(3) Воспроизведение** (рантайм).
   - Виола — узкое место только в (1)+(2). Поэтому: базу даёт **AI-style-модель** (генерит весь мир консистентно), риггинг — **AI авто-риг** или наш код, рантайм — наш движок.

4. **База ≠ человеко-конструктор.** Ready Player Me и подобные — **отвергнуто** (только люди, генерик-вид «как все», тянут 3D в PWA, бьются с 2D-питомцами). Правильная база = **AI-генератор, обученный на нашем стиле (Scenario)** → штампует аватар + питомцев + боссов + декор единообразно.

5. **Рантайм:** **SVG paper-doll СЕЙЧАС** (zero-dep, on-brand, уже в апке, доказан) → ship v1. **Rive** (~200КБ WASM) — апгрейд «сочности» ПОСЛЕ запуска: его супер-сила = **дата-байндинг** (анимация реагирует на живые данные: настроение Тени по энергии, HP босса по задачам). Конечная цель — «персонаж реагирует на твой прогресс».

### Как именно анимируем (механика)
- Каждый ассет — слои с известными id (см. `PET-PIPELINE.md`): тело/уши/хвост/глаза/слоты.
- Код применяет к слоям трансформы по классам/id: покачивание (дыхание), поворот предплечья (меч/отжимания), виляние, моргание, парение + частицы. **prefers-reduced-motion** уважается.
- Перекраска — через CSS-переменные/`fill`. Экипировка — SVG в слот-якорь. Всё уже работает в движке.

### Конкретные шаги ДЛЯ ТЕБЯ (Альберт), по порядку

**ФАЗА 0 — зафиксировать стиль (гейт, один раз; без неё всё на черновом арте):**
1. Собрать **6–10 эталонных иллюстраций в ОДНОМ chibi-стиле, ОДНА палитра**. Состав: 2–3 chibi-персонажа, 2–3 существа/питомца, 1–2 предмета/гир, опц. 1 кусок интерьера. Каждая: субъект целиком, фронт/¾, **плоский ровный свет**, чистая линия, простой/прозрачный фон, 1024px+. Путь: либо Виола рисует, либо ты генеришь image-моделью и куришь.
2. **Обучить Scenario-модель** на этих 6–10 → «Satoru style model». Это движок, который дальше штампует ВЕСЬ мир в одном стиле.

**ПИЛОТ (параллельно A/B, на ОДНОМ персонаже) — снять риск до объёма:**
- **A)** Виола рисует 1 персонажа **разобранным на слои** (paper-doll) → PSD/TIFF → я собираю cutout-риг + 2 анимации (idle + отжимания).
- **B)** Ты генеришь того же через Scenario + пробуешь **авто-риг** (Scenario 2D-rig sheet / godmodeai→Spine) → сравниваем.
- Что даёт хорошего анимируемого персонажа с меньшим трением — то и основной путь. Художница → стиль/курирование, НЕ блокер.

**ДАЛЬШЕ по фазам производства (`ART-PIPELINE.md`):**
- **Фаза 1 — один БОСС конец-в-конец:** генерим через style-модель → AI-анимация циклов (idle/hit/defeat) → я вшиваю спрайт-плеер в экран рейда. Макс. визуальный выхлоп, безопасный тест AI-пайплайна.
- **Фаза 2 — масштаб:** питомцы (по архетипам + морф-формы), мобы, эффекты. Батч-генерация через style-модель → ты куришь → код играет. (Движок видов/экипировки уже готов — просто подставляем реальные части вместо моих болванок.)
- **Фаза 3 — статика Логова:** генеришь **варианты комнаты** (обои/пол/окно/декор) + предметы мебели + иконки гира в стиле → я собираю комнату из кусков, слот-система мебели (вкл/выкл), свет по времени суток. ← ровно «генерация вариантов логова».
- **Фаза 4 — Rive реактивные герои:** компаньон (дата-байндинг к энергии/связи/настроению) → аватар (paper-doll, гир виден на теле, поза по энергии).

### Как закрываются именно твои пункты
- **Кастомизируемый персонаж в нужном стиле + его анимация:** заменить «PowerPoint»-аватар на chibi paper-doll — движок я строю (Фаза 3 PETS-EQUIPMENT-PLAN), части даёт style-модель/Виола, анимация — мои код-шаблоны.
- **Генерация вариантов Логова + разные виды:** ты генеришь арт-варианты через style-модель, я вшиваю в слот-сборку комнаты (Фаза 3 пайплайна).
- **Кастомизация животных + их анимация + итерация:** движок видов+экипировки+idle уже в апке; итерация = подставить реальные chibi-части + добавить шаблоны (happy/sleep/eat).
- **Всё вместе:** сцена Логова собирает аватар + питомцев + Тень + гир из ОДНОГО paper-doll движка и ОДНОЙ style-модели → единый мир.

### Что могу я vs что нужно от тебя
- **Я (код):** все движки (paper-doll, слоты, перекраска, шаблоны анимаций), сборка комнаты, свет, спрайт-плеер, Rive-интеграция позже; могу рисовать **черновые programmer-chibi SVG** как болванки, чтобы механика жила до прихода арта.
- **Ты:** Фаза 0 (собрать style-seed + обучить Scenario), генерация/курирование арта, пилот A/B. Виола: рисует героев по слоям + курирует (опц.).

### Узкое место и первый ход
Код почти готов; **первый ход — Фаза 0** (style-seed 6–10 → Scenario-модель), потом пилот A/B на одном персонаже. После этого весь downstream = «сгенерил в стиле → подставил в движок, который уже построен».
