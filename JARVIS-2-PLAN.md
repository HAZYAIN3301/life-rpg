# Джарвис-2 — Тень как ОДИН голос (план, Фаза A сделана)

> 2026-07-26. Продолжение Джарвис-1 (DEVLOG 26.07: чат/предложения видят состояние). Консолидация нудж-карточек «Сегодня» в один голос Тени. **✅ Фаза A сделана в тот же день** (по команде Альберта «что уже можем строить» — единственный кусок, не требующий его решений). Фазы B/C и вопросы 1–3 ниже всё ещё ждут ответа. Подробности реализации Фазы A — DEVLOG 26.07.

## Диагноз: девять карточек борются за одно внимание

Инвентаризация условных карточек на «Сегодня» (`renderToday`, порядок как в шелле):

| # | Карточка | Триггер | Характер |
|---|---|---|---|
| 1 | `lowEnergyNudge` 🪫 | энергия <25% и есть сделанное | забота |
| 2 | `restNudge` 🌿 | ≥4 дней без явного отдыха (и ≥5 задач всего) | забота |
| 3 | `nudgeCard` 🎁⚡🔥 | сундуки ждут / буст / Хайп активен | награда |
| 4 | `entryNudge` 🕯 | вечер/низкая энергия + любимые сферы + ≥90 мин тишины | спасение момента |
| 5 | `dayLogNudge` 🎤 | ≥2 тихих дня ИЛИ вечер без единого дела | захват |
| 6 | `importNudge` 🎖 | нет импорта и <200 XP | онбординг |
| 7 | `stretchNudge` 🤸 | ≥4 ч сидячих планов без движения | забота |
| 8 | `mobilityNudge` 🧘 | силовые/единоборства без растяжки (opt-out) | забота |
| 9 | `sysTeaser` ⚡ | ур.2+, режим Система не открыт | дискаверабилити |

Худший реальный случай: у уставшего юзера вечером могут одновременно гореть 1+2+4+5+7 — **пять советов подряд перед списком квестов**. Это прямо кормит fb #9 «панель нечитаема» и противоречит собственному принципу «Заход» задизайнен как ОДНО тёплое приглашение. И это же — анти-паттерн секретаря: секретарь не зачитывает девять памяток, он говорит одну фразу о главном.

Не трогаем: `overdueCard` (это данные, не совет), `companionCard` (дом Тени — голос будет жить именно тут), баннер установки.

## Концепция: приоритетный селектор + голос Тени

**Слой 1 — селектор (чистый JS, ноль ИИ-стоимости).** `nudgeSignals()` собирает все сработавшие сигналы из таблицы → сортировка по приоритету → **показывается один** (плюс, отдельно, `nudgeCard` с сундуками — см. вопрос №1). Черновик приоритета (спасение момента > честность > захват > забота > онбординг > витрина):

`entry (4) > rest (2) > dayLog (5) > lowEnergy (1) > stretch (7) / mobility (8) > import (6) > sysTeaser (9)`

Проигравшие сигналы не исчезают навсегда — они естественно всплывут, когда главный погаснет (заход сделан → на следующем рендере говорит отдых). Плюс тай-брейк по «давности показа» в `settings.nudgeSeen` (сигнал, который давно не показывался, получает шанс), чтобы низкоприоритетные (import, sysTeaser) не были заперты навечно.

**Слой 2 — озвучка Тенью (при ключе; фолбэк = текущие статичные тексты).** Выбранный сигнал уходит в один короткий ИИ-вызов: «ты — Тень, вот сигнал (отдых: 6 дней), вот срез (`stateNowContext()`), скажи ОДНУ тёплую фразу ≤120 знаков, без вины, кнопку не переписывай». Текст кэшируется в `settings.nudgeVoice = {sig, text, at}` и перегенерируется только при смене сигнала или раз в N часов — **не на каждый рендер**. Кнопка-действие остаётся родной (`data-action` как сейчас) — ИИ красит слова, не логику. Без ключа/Free — статичный текст сигнала, как сегодня: фича деградирует, не ломается (гейт из AI-STRATEGY соблюдён).

**Слой 3 (отдельный шаг, после A+B) — «он делает»: карточки-действия в чате.** Помощник отвечает предложением с кнопкой («поставить „Черновик JuFo-постера" на завтра? ✓») — реюз движка Предложений внутри чата + расширение `applyProposals` типом `quest` (сейчас умеет только сферы/уровни/цели). Это самый «вау»-кусок, но и самый объёмный — не тащить в один заход с A+B.

## Почему это не «ещё одна фича перед запуском»

1. **Чинит существующий баг восприятия** (fb #9 «нечитаемо»), а не добавляет поверхность.
2. **Витрина всего, что построено за три сессии**: честный отдых, энергия, Заход, захват — начинают говорить одним голосом, «Джарвис» становится видимым за один экран.
3. **Дёшево по компьюту**: ≤1 короткий вызов в несколько часов, только при ключе, только когда сигнал есть. Ложится на линию Free/Pro (LAUNCH-KIT A1) без изменений.

## Этапы и проверка

- **✅ Фаза A — селектор без ИИ.** Сделано 26.07: `pickNudge()` перед `renderToday`, ≤1 совет-карточка за рендер (`nudgeCard`-сундуки отдельно), тай-брейк давности через `settings.nudgeSeen` — но только для объявленной в плане коллизии (stretch/mobility), а не полное анти-голодание для всех уровней (см. DEVLOG почему это разумное упрощение). Проверено: 5 синтетических случаев на `pickNudge()` напрямую + живой рендер с 12 сидированными задачами (5 потенциальных сигналов → ровно 2 карточки на экране). Детали — DEVLOG 26.07.
- **Фаза B — озвучка Тенью.** Мок-fetch проверка промпта/кэша (как Джарвис-1), живой вызов — у Альберта. Анти-спам: смена текста только при смене сигнала. **Ждёт ответов на вопросы 2–3 ниже.**
- **Фаза C — действия в чате.** Отдельный план после A+B.

## Открытые вопросы (нужен Альберт)

1. **Сундуки (🎁 `nudgeCard`)** — ✅ решено реализацией Фазы A: оставлены отдельной карточкой (моя рекомендация была именно эта). Если хочешь иначе — скажи, переигаю.
2. **Где живёт голос** — отдельная карточка на месте нуджей (минимум работ) или внутри `companionCard` рядом с Тенью (красивее, но трогает вёрстку компаньона перед дизайн-системой)? Моё предложение: v1 — на месте нуджей, перенос в компаньона — вместе с дизайн-системой.
3. **Частота перегенерации** ИИ-текста: раз в 3 часа? раз в сигнал?
4. ~~Начинать ли Фазу A сразу~~ — ✅ сделано 26.07.
