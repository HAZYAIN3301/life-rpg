# Recovery Guardian runtime v1 — QA

Status: **PASS**.

- 4/4 state plates: 1024×1024 RGBA, non-empty alpha, transparent corners;
- idle-softbody.gif: 33 frames on a 1024×1024 canvas;
- runtime state derivation and markup contract covered by the Node suite;
- decoded crossfade prevents blank frames;
- Den stage uses a square physical box and the deterministic `mid-east` slot;
- room population remains bounded by the configured pet count;
- contact animation is intentionally absent until authored pair frames exist.
