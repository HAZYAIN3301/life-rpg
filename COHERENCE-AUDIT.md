# Связность приложения — карта и дыры

> Составлено 2026-09-02 по брифу `HANDOFF-COHERENCE-AUDIT.md`.
> Метод: приложение поднято из `~/Projects/life-rpg` (`PORT=4318 node server.js`), пройдено
> вживую под QA-аккаунтом «Тестовый герой» — от чистого аккаунта до созданной цели,
> заведённого правила внимания и закрытого квеста. Каждое утверждение ниже либо
> воспроизведено в браузере, либо подтверждено строкой кода. Впечатлений здесь нет.
>
> ⚠️ Ловушка, на которую я потратил час: `.claude/launch.json` указывал на мёртвую копию
> внутри Obsidian-хранилища, и первый проход я сделал по ней. Путь исправлен, но
> `preview_start` кэширует конфиг — проверяй `ps ax | grep server.js` перед выводами.

---

## 1. Куда можно дойти

Навигация трёхуровневая, и это уже часть проблемы: раздел → под-вкладка → вкладка внутри
экрана. Ни один экран не показывает, что уровней три.

| Раздел | Под-вкладки | Гейт | Как попадают |
|---|---|---|---|
| Сегодня | День · Заметки | — | по умолчанию |
| — | *внутри «Дня»*: День · **Доска** | — | третий уровень, `data-action="today-tab"` |
| План | Календарь · Цели | — | верхняя навигация |
| Привычки | Привычки | — | верхняя навигация |
| Вдохновение | Подборка | — | верхняя навигация |
| Награды | Награды | — | верхняя навигация |
| Герой | Логово · Персонаж · Питомцы · Навыки · Прогресс | **уровень 3** | заблокировано до 3-го уровня |
| Племя | Пати · Рейтинг | **уровень 3** | заблокировано до 3-го уровня |
| Настройки | Аккаунт · Опыт · Жизнь · Связи · Прогресс · Данные | — | шестерёнка |

`public/app.js:25205` — `SECTIONS`; гейты `gate: 3` там же.

**Поверхности, до которых нельзя дойти, не зная заранее, что они есть:**

1. **Внимание и границы** — единственное место, где заводится правило, это
   Настройки → Опыт → 📲 Приложение → «Внимание и границы» (`public/app.js:15958`).
   На «Сегодня» вход есть, но он спрятан внутри свёрнутой по умолчанию «Другой поддержки»
   Тени. Пока правил ноль, о существовании подсистемы не говорит ничего.
   Это и есть «Attention — я вообще этого не видел».
2. **Founder Pass** — Настройки → Аккаунт, между подпиской и админкой (`public/app.js:24678`).
   Карточка опроса о готовности платить лежит там, куда заходят менять пароль.
3. **Доска** — третий уровень внутри «Сегодня». Из верхней навигации её не видно.
4. **Логово, Питомцы, Персонаж, Навыки, Прогресс, Пати, Рейтинг** — семь поверхностей
   закрыты до 3-го уровня. Тень при этом здоровается с человеком на «Сегодня» с первой
   минуты: компаньон есть, а дома у него нет.
5. **Демо-страницы** — `avatar-demo.html`, `paperdoll-demo.html`, `pet-cat-demo.html`,
   `fortune-rig-v2-demo.html`, `shadow-rig-v2-demo.html`, `avatar-rig-demo.html`,
   `avatar-forge-v1.html` отдаются сервером, но ссылок на них в приложении нет.

---

## 2. Кто что производит и кто это читает

| Часть | Производит | Кто читает | Как входят / куда выходят |
|---|---|---|---|
| Квесты дня | `tasks[]`, XP, золото, `actualMin` | Прогресс, Награды, Рейтинг, Тень, Календарь | «Сегодня» → остаётся там |
| **Цели** | `goals[]`, `steps[]`, дедлайн | Экран «Цели», Гид, контекст ассистента, а с 02.09 — день (чип цели на квесте) и календарь (дедлайны). До правок ни `renderToday`, ни `renderCalendarView` не обращались к `State.goals` **ни разу** | План → Цели → день и календарь |
| Привычки | `habitlog`, XP | Прогресс, «Сегодня» | Привычки ↔ Сегодня — связь есть |
| Заметки / инбокс | `inbox[]` | `noteToQuest()` (`public/app.js:16214`) превращает заметку в квест дня с `noteSourceId` | Сегодня → Заметки → **обратно в день. Работает.** |
| Внимание | `policies`, `sessions`, `episodes` — по умолчанию в **localStorage** (`satoru_attention_v1:<user>`); серверная синхронизация включается отдельным выбором в том же диалоге | Тень (утренний возврат), Полка возвращения | Настройки → правило → эпизод → Тень |
| Уговоры (`CommitmentV1`) | границы **квестов** в `settings.commitmentsV1` | меню квеста, Контроль | ••• на квесте → там же |
| `commitment-v2.js` (вид `attention`) | — | **никто: файла нет ни в `index.html`, ни в `SHELL`** | входа нет |
| Движок Секретаря (`secretary-router/events/claim/experiment` + 6 ручек сервера) | ход дня, журнал событий, 30-дневный эксперимент | **никто: `grep -c 'api/secretary' public/app.js` = 0** | входа нет |
| `POST /api/attention/episode` | — | **никто: `grep -c 'api/attention/episode' public/app.js` = 0**; клиент шлёт весь конверт через `PUT /api/attention` | входа нет |
| Закрытие дня | `days[date].closed`, `reflection` | недельный обзор, ИИ-контекст, Тень | «Завершить день» → тупик |
| Доска | заказы, вкус (`board-taste`) | сама Доска; с 02.09 — карточка квестов дня (взятое видно и открывается одним тапом) | Сегодня → Доска → обратно в день |
| Логово / питомцы / компаньон | `bond` | сцена Логова | растёт **только** от чек-ина и «захода» (`public/app.js:7276`, `25928`, `28394`) — от целей, привычек и квестов не растёт |
| Founder Pass | ответ об оплате | сервер | Настройки → Аккаунт |
| Опросник онбординга | цель + **первый шаг как квест на сегодня** (`public/app.js:13111–13113`) | день читает квест | единственный раз, при регистрации |

---

## 3. Дыры, по цене для человека

Дыра — место, где действие человека должно было перетечь в следующее, а закончилось
тупиком и необходимостью самому вспомнить, куда идти.

### №1. Цель не доходит до дня, а день не знает про цели. **(закрыта, см. §4)**

Самая дорогая, потому что это позвоночник продукта, и потому что приложение **само задаёт
нужный вопрос и само выбрасывает ответ.**

Воспроизведено: создаю цель, в форме есть поле «Первый шаг — конкретное действие, с
которого начнёшь», пишу «Написать абзац о методе». Открываю «Сегодня»:

> **Соберём первый квест на сегодня.** Начни с одного понятного действия… **На сегодня пусто.**

Первый шаг стал пунктом чек-листа цели (`public/app.js:26737`: `steps: !metric && firstStep ? [...]`),
а не квестом. Чтобы он попал в день, надо было открыть цель, раскрыть свёрнутый «План
цели», найти форму «Квест на сегодня» и **напечатать ту же фразу второй раз**
(`public/app.js:26765`).

Три доказательства, что это дефект, а не замысел:

- `goalNextAction()` (`public/app.js:19905`) возвращает `kind: 'task'` или `kind: 'step'`;
  задача рисуется ссылкой в день, а шаг — мёртвым `<span>`. Экран «Цели» показывает
  «Следующий шаг», по которому нельзя сделать шаг.
- Опросник онбординга делает ровно правильную вещь: `task: { date: todayStr(), goalId: goalSeed.localId }`
  (`public/app.js:13111`). Приложение знает, как надо, — и делает это один раз в жизни аккаунта.
- Заметка умеет становиться квестом с защитой от дубля (`noteToQuest`, `noteSourceId`).
  Шаг цели — единственный из трёх источников действия, у которого моста не было.

### №2. Движок Секретаря построен и ни к чему не подключён

`secretary-router-v1.js`, `secretary-events-v1.js`, `secretary-claim-v1.js`,
`secretary-experiment-v1.js`, шесть серверных ручек, 83 зелёных теста — и **ноль вызовов из
клиента**. `commitment-v2.js` (вид `attention`, граница `duration` — «в TikTok только
выложить, двенадцать минут») не подключён ни в `index.html`, ни в `SHELL`.

Цена: «домашний Джарвис» пока живёт двумя половинами, которые не разговаривают. Утренний
ход выбирает параллельный локальный детектор в `app.js`, а не Router.

Уже расписано и распределено: `HANDOFF-CODEX-SECRETARY-UI.md` (UI-агент) и `BACKLOG.md`
(producer-адаптеры, ждут подтверждения Альберта). **Не трогал — чужая работа в работе.**

### №3. Граница внимания заводится один раз и никуда не течёт

Правило по умолчанию живёт в `localStorage` (`mode: 'local'`); серверная синхронизация — это
отдельный выбор в том же диалоге (`storageMode` → `mode: 'contracts'`, `PUT /api/attention`
целым конвертом). Инкрементальная ручка `POST /api/attention/episode` не вызывается из
клиента **ни разу** — построена и мертва. Значит:
граница не переживает смену устройства, не становится уговором (`commitment-v2` не
подключён), не порождает события для Секретаря и не видна ни в Прогрессе, ни в Наградах.
Подсистема, до которой Альберт не дошёл, — ещё и подсистема, из которой ничего не вытекает.

Это та же дыра, что №2, с другого конца. Чинить их надо одним движением, а не двумя.

### №4. Из дня не видно, какой цели служит квест. **(закрыта, см. §4)**

Связь была, но спрятана: ссылка «🎯 Открыть связанную цель» лежала внутри меню `•••`
конкретного квеста. На самом экране дня слово «цель» не появлялось ни разу — день читался
списком поручений, а не движением к чему-то.

Дешевле №1, поэтому ниже: человек хотя бы мог дойти, если вспоминал про `•••`.

### №6. Календарь не знал про дедлайны целей. **(закрыта, см. §4)**

Найдена позже первых пяти: при первом проходе я не открыл Календарь — это моя недоработка,
дыра оказалась крупнее №4. `renderCalendarView` не обращался к `State.goals` **ни разу**, как
и `renderToday`. Дедлайн — дата, назначенная самим человеком; календарь — единственное место
в приложении, где живут даты; и они не встречались. `targetDate` читали только сама цель
(`goalDeadlineHTML`), контекст ассистента и Гид.

Цена: срок, поставленный в одном месте, дальше существовал только в памяти человека — хотя
календарь лежит в соседней под-вкладке того же раздела «План».

### №5. Логово и питомцы — поправка к первому проходу

⚠️ Первый проход утверждал, что Логово и питомцы «не следуют из жизни». Полный проход это
**опроверг наполовину**, и утверждение было моей ошибкой из-за того, что я не открывал эти
экраны: гейт 3-го уровня, а тестовый аккаунт был на первом.

- **Питомцы следуют.** «Питомцы показывают ритм основных сфер жизни» — состояние каждого
  считается по XP этой сферы за десять дней (`petStats`). Голодный питомец = запущенная
  сфера. Это связь, и довольно прямая.
- **Логово следует.** Оно читает день: показывает счёт квестов, предлагает «Начать фокус»,
  уводит в Награды и к Питомцам.
- **Не следует ровно одно — `bond` спутника.** Он растёт от чек-ина, «захода» и поглаживания
  и не растёт ни от закрытой цели, ни от удержанной границы, ни от серии привычки.

Что должно питать связь — по-прежнему продуктовый вопрос.

`bond` компаньона растёт от трёх событий: чек-ин, «заход», ручное поглаживание
(`public/app.js:7276`, `25928`, `28394`). Ни закрытая цель, ни серия привычки, ни удержанная
граница внимания в Логово не приходят. Это отдельная жизнь внутри приложения, а не
следствие того, что человек делает. Плюс гейт 3-го уровня: Тень здоровается сразу, а дом
её появляется потом.

**Не трогал: это продуктовый вопрос (правило 7), а не сломанный провод.** Что должно
питать связь — решает Альберт.

---

## 4. Что закрыто в этой сессии

**Дыра №1, целиком, с обеих сторон.**

- `public/goal-step-quest-v1.js` — чистый модуль (без DOM, `State`, сети и часов):
  `planStepQuest` собирает квест ровно тех же значений, что и ручная форма «Квест на
  сегодня»; `questForStep` не даёт появиться второму квесту на тот же шаг;
  `applyStepOutcome` отмечает шаг закрытым квестом.
- В цели у каждого невыполненного шага появился один ход — **«В день»**; тот же ход
  появился в карточке «Следующий шаг», которая раньше была мёртвым текстом. Шаг, уже
  стоящий в дне, показывает «Уже в дне» и ведёт к своему квесту.
- Обратный ход: закрытый квест отмечает свой шаг, отменённый — снимает отметку. Снимается
  **только та отметка, которую поставил сам квест**: галочку, поставленную человеком
  руками, откат квеста не отменяет.
- Живая проверка: «На сегодня пусто» на «Сегодня» сменилось на «Следующий ход уже
  выбран → Начать: Написать абзац о методе»; закрытие квеста отметило шаг и завершило цель;
  отмена вернула оба состояния; ручная галочка пережила оба перехода.

**Дыра №4, обратным ходом к той же связи.**

- `public/quest-goal-link-v1.js` — чистый модуль: `linkFor` отвечает, что честно сказать про
  один квест, `linksFor` резолвит целый день за один проход. Правило одно — **молчать там,
  где нечего утверждать**: квест без цели, оборванная ссылка на удалённую цель и цель без
  названия дают `null`, а не значок.
- На строке квеста под названием появился чип с названием цели; клик открывает эту цель.
  Чип встал в ту же ячейку, где уже живёт «Ядро дня», поэтому сетка строки не изменилась.
- Состояние цели проговаривается **словом**, а не только цветом: «цель на паузе»,
  «цель ждёт», «цель завершена», «цель в архиве». Архив сильнее завершения, завершение
  сильнее статуса — закрытая и убранная цель не имеет права выглядеть живой работой дня.
- Побочно найден и починен старый дефект вёрстки: ячейка названия квеста — вложенная сетка
  с колонкой `auto`, она растягивалась по самому широкому элементу и вылезала за правый край
  строки. На `375px` слово о состоянии цели обрезалось краем карточки. Теперь длинное
  название сжимается многоточием, а слово о состоянии остаётся целым.

**Дыра №6, во всех трёх режимах календаря.**

- `public/goal-deadline-calendar-v1.js` — чистый модуль: `deadlinesByDate` строит индекс
  «дата → дедлайны этого дня» один раз на отрисовку. Календарно невозможная дата
  (`2026-02-31`) днём не становится; архивные и достигнутые цели в план будущего не попадают —
  тем же фильтром, каким их уже отбирает контекст ассистента, чтобы один факт не описывался
  двумя способами. Порядок внутри дня детерминированный и не зависит от порядка в файле.
- День, неделя и месяц помечают дни с дедлайном — значком и словами в `aria-label`, — а сама
  цель открывается из шапки выбранного дня. Ссылку внутрь кнопки выбора дня вкладывать нельзя
  (ломается клавиатура и разметка), поэтому маршрут читается как **пометка → день → цель**.
- Три режима сделаны за один заход намеренно: закрыть один значило бы оставить тупик на
  переключении вкладки, то есть не закрыть дыру.
- **Внешний календарь остался без дедлайнов, и это сделано осознанно.** Экспорт `.ics`
  (`app.js`) и живая подписка (`server.js`) до сих пор отдают только квесты со временем. Дыра
  та же самая, на шаг дальше — в настоящий календарь человека. Но починить её наполовину
  нельзя: разойдись эти два источника, и один и тот же дедлайн начнёт описываться двумя
  способами, а вторая половина лежит в `server.js`, то есть у агента движка. Отдельным
  пунктом в `BACKLOG.md`.

Новых экранов, вкладок, дашбордов, XP, золота и наград не добавлено ни в одной из трёх правок.

---

## 4b. Построено, покрыто тестами и недостижимо

Второй проход, уже не по экранам, а по файлам: какие модули собраны и **не подключены к
`index.html`**. Это первый из двух видов разрыва по брифу — «функция, чей результат не читает
никто», — и в самом чистом виде: результат не читает никто, потому что функции нет входа.

```
for f in public/*-v1.js public/*-v2.js; do b=$(basename $f);
  grep -q "$b" public/index.html || echo "NOT LOADED: $b"; done
```

**Сразу исключаю то, что не является дырой.** `bulk-undo-v1`, `commitment-journal-v1`,
`goal-resolve-v1` живут на сервере (`require` в `server.js`) — им и не место в браузере.
`avatar-forge-v1`, `browser-companion-landing-v1` — отдельные страницы. Шесть модулей
`traveller-palette-*` / `traveller-look-v2` / `traveller-frame-registry-v2` /
`traveller-appearance-controller-v2` **намеренно dormant**: DEVLOG называет и причину, и
ворота — «пока не готовы и не утверждены все 92 маски». Спящий по названной причине модуль —
это план, а не дыра.

Остаётся **два** (проверку «ноль вызовов ≠ мёртвый код» и почему `den-resident-life-v1` в этот
список не попал — см. §4g.1), и оба отвечают на записанные вопросы Альберта:

| Модуль | Что умеет | Откуда взялся |
|---|---|---|
| `arena-audit-v1` | Соперник как упаковка: мишень, темп, свидетель, ставка — по отдельности | `DISCIPLINE-ARENA-PLAN` §5, дословный вопрос «как сделать с учёбой, если я лучший в классе?» |
| `rest-profile-v1` | Рецепты отдыха с нулевой ценой входа | `SECRETARY-OS-PAIN-MAP` BH-02, разбор 01.09 — «я не знаю, как отдыхать» |

✅ **`sphere-frequency-v1` из этого списка вышел** — подключён и вызывается с 02.09
(`public/app.js:22664`). Строка в таблице снята, чтобы список не врал о собственном размере.

⚠️ **Про два из них это уже писали.** `DEVLOG.md` (запись от 2026-08-10, строка 783):
«`arena-audit-v1` и `sphere-frequency-v1` построены, покрыты тестами и не подключены к
рантайму — два пункта брифа лежат готовыми в темноте». С тех пор прошёл месяц и не
изменилось ничего. Это ровно тот исход, о котором предупреждает бриф: отчёт, после которого
никто ничего не сделал. Поэтому здесь не наблюдение, а вопрос с двумя вариантами (§6).

## 4c. Поля, которые приложение спрашивает и не читает

Второй вид того же разрыва: ответ человека сохраняется и не используется **нигде**, кроме
поля, в которое он был введён.

| Поле | Вопрос, который задаёт приложение | Кто читает |
|---|---|---|
| `settings.identityGoal` | «Кем ты хочешь стать?» — флагманский вопрос Atomic Habits | только собственный `<input>` (`app.js`, `save-identity` пишет, одна строка читает) |
| `habit.atomic.identity` | «Я — человек, который…» | только собственный `<input>` |
| `habit.atomic.cue` | «После [привычки] я…» | только собственный `<input>` |
| `habit.atomic.twoMin` | «Версия 2 минут — минимум, чтобы просто начать» | собственный `<input>` и обязательное поле в Гиде |

Особенно заметен последний. Экран привычек **сам советует**: «Возвращайся с версии на две
минуты» — и не показывает, какая версия у человека записана. Совет отсылает к собственному
ответу человека, которого нигде не видно.

Чинить это без Альберта нельзя: любое место, где эти слова появятся, — решение о том, что он
видит и каким тоном (правило 7). Разница с уже закрытыми дырами существенная: там связь уже
существовала в данных и была спрятана в подменю; здесь её не существовало нигде.

---

## 4d. Нить целиком: проход после работы

Проверка не по коду, а тем же способом, что и в начале, — проход по приложению глазами
человека, уже после всех правок. Он и нашёл последний разрыв, которого не видно в файлах.

**День говорил две разные вещи о том, что сейчас главное.** Герой: «Следующий ход уже
выбран. Сейчас лучше не смотреть на всю систему — закрой следующий квест» → *Написать абзац
о методе*. И тут же, ниже, Тень: «**Сейчас важнее всего** — Сил нет и не надо много, есть
вход на 10 минут в любимое». Два блока на одном экране объявляли себя главным и указывали на
разное; собственный совет героя опровергался следующей же карточкой.

Починено не арбитражем, а честностью заголовка: право назвать себя главным осталось у
**заявленного хода движка** — он про то, что случилось с человеком. Совет получил то слово,
которым его уже называет соседняя строка той же карточки, — «Поддержка». Тиры нуджей
(их порядок откалиброван не здесь и с аргументами в комментариях) не тронуты; совет
показывается ровно так же, но больше не спорит с героем. Пинится тестом: на экране ровно
один претендент на «важнее всего».

### Как нить идёт сейчас

| Решение человека | Куда доходит | Что возвращается обратно |
|---|---|---|
| Цель и её первый шаг | квест сегодняшнего дня | закрытый квест отмечает шаг, шаг закрывает цель |
| Дедлайн цели | день, неделя, месяц календаря | цель открывается из выбранного дня |
| Квест | чип цели прямо в строке дня | цель открывается одним тапом |
| Граница внимания | эпизод → событие на сервере | утренний ход Тени на «Сегодня» |
| Привычка | список дня; версия на две минуты, когда серия оборвана | дни касания → ритм сферы в Прогрессе |
| Частота сферы | ритм в Прогрессе | «реже, чем ты решил» с именем сферы |
| Заметка | квест дня (`noteSourceId` от дубля) | — |
| Заказ доски | строка в карточке квестов | один тап открывает именно этот заказ |
| Материал Полки | открывается через границу внимания | завершается переходом в квест, цель или заметку |
| «Кем ты хочешь стать» | заголовок экрана Привычки | — |

### Что осталось разрезанным

1. **Логово, питомцы, компаньон** (§5) — `bond` растёт только от чек-ина, «захода» и
   поглаживания. Продуктовый вопрос: что должно питать связь.
2. **Словарь.** Одна и та же вещь названа по-разному на соседних экранах — см. §4e.

## 4g. Карта фактов: систематический проход

До этого все дубликаты находились попутно — я спотыкался о них, проходя по экранам. Здесь
проход по **тому, что приложение хранит и считает**, а не по тому, что оно показывает.
Метод, чтобы его можно было повторить:

1. Перечислить всё хранимое: 27 ключей `DEFAULT_SETTINGS`, файлы `USER_DATA_FILES`,
   выделенные хранилища (`attention`, `shelf`, `secretary-*`, `bulk-ledger`, `founderpass`,
   `questionnaire`, `board-*`).
2. Для каждого — кто пишет, кто читает, есть ли **второй** писатель.
3. Для каждого чистого модуля — зовёт ли его хоть кто-нибудь (клиент, сервер, другой модуль).

### Чего проход НЕ нашёл — и это тоже результат

- **Ни одного ключа настроек без читателя.** Все 27 читаются. Осиротевших полей в
  `settings` нет.
- **Ни одного выделенного хранилища без пары писатель/читатель.** `secretary-events`,
  `secretary-ledger`, `secretary-claims`, `secretary-experiment`, `bulk-ledger`, `shelf`,
  `attention` — у всех есть и то, и другое. Единственный дефект этого класса — Router,
  читавший несуществующий `commitments.json`, — найден и закрыт 03.09 (см. DEVLOG).
- **Board taste и интересы Вдохновения — разные факты, а не дубликат.** Первое про то, какие
  заказы откликаются, второе про темы материалов. Сводить их было бы ошибкой.

### Что нашёл

**1. ~~`den-resident-life-v1.js` не вызывается никем.~~ Ошибка — снято 03.09.**

Я записал его четвёртым «модулем в темноте»: `grep` по всему `public/`, `server.js` и
`scripts/` не нашёл ни одного вызова `DenResidentLifeV1`. Вывод был неверный. Модуль
**сам себя ставит**: на загрузке он подписывается `MutationObserver` на `documentElement`,
сам находит обитателей и запускается. Вызывающий ему не нужен. Проверено вживую на Логове:

> `DenResidentLifeV1.inspect()` → `{"busy":false,"step":0,"connected":true}`

`connected: true` — он подключён к сцене и работает. Обитатели моргают.

**Методическая поправка, важнее самой находки:** «ноль вызовов» в этой кодовой базе **не
означает мёртвый код**. Часть модулей — самоустанавливающиеся. Проверять надо двумя шагами:
grep по вызовам **и** grep по `MutationObserver|DOMContentLoaded|addEventListener('load'|
setInterval|requestAnimationFrame` внутри модуля. Прогнал так все четыре «модуля в темноте»:

| модуль | вызовов | признаков самозапуска | вывод |
|---|---|---|---|
| `den-resident-life-v1` | 0 | 3 | работает сам — не в темноте |
| `sphere-frequency-v1` | 0 | 0 | в темноте (подключён 02.09) |
| `arena-audit-v1` | 0 | 0 | в темноте |
| `rest-profile-v1` | 0 | 0 | в темноте |

Список §4b остаётся: их три, а не четыре.

**1b. Исчерпывающий прогон — и чистый отрицательный результат.**

Найдя ошибку в методе, прогнал его исправленным по **всем** скриптам `index.html`, а не по
тем, о которые споткнулся. Каждому — имя глобала, число вызовов вне самого файла, признаки
самозапуска. Результат:

- **Ни одного модуля, который грузится у всех и при этом не работает.** Единственный ноль
  вызовов — `den-resident-life-v1`, и он запускается сам.
- `arena-audit-v1` и `rest-profile-v1` **вообще не в `index.html`** — они не «висят мёртвым
  грузом», они просто не доехали до браузера. Файл с тестами есть, входа нет. Это §4b, и
  стоит дешевле, чем я думал: байт они не стоят никому.

То есть байты на каждом запуске ни за что не платятся нигде. Это стоило проверить, и это
стоит записать: пусто — тоже ответ.

**2. «Сделано сегодня» считается дважды, и оба числа стоят на одном экране.**

Показано вживую, детерминированно: отметил **одну привычку** и получил

> герой: «**1** СДЕЛАНО СЕГОДНЯ»  ·  карточка квестов: «Квестов: **0/0**»

- герой берёт `dayLoadNow()` → `arenaDayHistory`: закрытые квесты (в день **выполнения**),
  **плюс привычки**, **плюс достигнутые цели**;
- карточка берёт `todays.filter(done)` — только квесты с сегодняшней датой.

⚠️ **Классифицирую честно: это не два производителя одного факта, а два разных факта под
неразличимыми именами.** Оба числа верны в своих границах. Дефект в том, что «сделано
сегодня» не говорит, что именно оно считает, и человек читает два числа про свой день как
противоречие. Поэтому это семейство §4e (имена), а не §4f (калькуляторы), и лечится словами,
а не слиянием: сказать, что считает каждое.

**3. У «Уговоров» построена только половина: писать умеет, отвечать — нет.**

Это самое крупное, что нашёл проход, и оно ровно про связность: факт производится,
он верен, он покрыт тестами — и не доходит ни до одного потребителя.

`commitment-v1.js` отдаёт 18 функций. Интерфейс зовёт девять, и все девять — **запись**
и жизненный цикл:

| зовётся из app.js/server.js | не зовётся ниоткуда |
|---|---|
| `normalize` (12), `release` (6), `revise` (2), `archive` (2), `reopen` (2), `mark` (2), `add` (1), `clearMark` (1), `outcomeOf` (1) | `streakOf`, `dayScore`, `unsettled`, `dueOn`, `coreOf`, `extrasOf`, `activeItems`, `setMode`, `emptyState` |

Слева — «завести уговор, пересмотреть, снять, отметить, закрыть». Справа — **всё, что
отвечает на вопрос «как я держусь».** `streakOf` считает серию *вместе с бюджетом промахов*
и отдельно возвращает `recorded`; `dayScore` — победы/промахи/молчание за день; `unsettled` —
какие уговоры сегодня ещё без ответа, и комментарий в модуле объясняет, почему это сделано
мягко: спрашивать про всё подряд — «значит превращать заботу в допрос». Ни одна из них не
вызвана.

Проверил и обратное: app.js не считает это сам, по-своему. Он просто не показывает. Весь
след уговора в интерфейсе — одна строка в меню квеста:

> ⚔️ Граница: 07:30 · [Пересмотреть] [Снять бесплатно]

⚠️ **Это не дубликат и не второй производитель — это производитель без потребителя.**
Смысл уговора с бюджетом промахов в том, что он **прощает**: два промаха за десять дней — ты
всё ещё держишь слово. Человек этого не видит никогда. Он видит границу и кнопку «снять».
Из двух половин уговора построена строгая, а добрая — та, ради которой всё и затевалось, —
лежит непоказанной.

Стоит рядом с тем, что я чинил сегодня: граф уговоров достаточно силён, чтобы **запереть
запись на всех устройствах сразу** (баг 03.09), и при этом не отдаёт человеку ни одного
числа о себе. Вся цена — и ни одного ответа.

Строить не мне: `commitment-*` за агентом движка, `app.js` за Codex. Передаю как находку.

**3b. И — почему её нельзя просто «показать»: гейт честности в `streakOf` не может сработать.**

Я собирался написать модуль-переводчик (факт → слова), как `secretary-offer-view-v1`. Прежде
чем строить, проверил сам факт — и строить не стал.

Докстринг `streakOf` объявляет гейт: рядом с серией отдаются `recorded` и `covered`, «чтобы
интерфейс не мог показать „14 подряд“, умолчав, что записано три дня из четырнадцати. Этика
„не наказывать“ не даёт права на приятную неправду». Гейт правильный. Данных для него нет:

```js
const mk = days => days.reduce((s,d) => C.mark(s,'wake',d,'win'), base);
C.streakOf(mk(['2026-08-23','2026-08-24','2026-08-25']), 'wake', '2026-08-25')
C.streakOf(mk(['2026-08-21','2026-08-23','2026-08-25']), 'wake', '2026-08-25')
C.streakOf(mk(['2026-05-10','2026-06-15','2026-08-25']), 'wake', '2026-08-25')
```

Все три возвращают **побайтово одно и то же**: `{streak:3, recorded:3, covered:120, forgiven:0}`.
Три дня подряд, три через день и три раза за три с половиной месяца неразличимы. Причина в
том, что `covered` считает не длину серии, а число просмотренных дней: при непрерывной серии
это всегда `lookback`, то есть 120. Молчаливые дни **внутри** серии в записи не остаются.

`commitment-v2` наследует это ровно: `commitment-v2.test.js:108` требует `deepStrictEqual` с v1.

⚠️ **Поэтому любой интерфейс поверх этой записи сказал бы «3 подряд» человеку, который сделал
это три раза с мая.** Ровно та приятная неправда, которую модуль сам себе запретил, и ровно
то, против чего написан `ALTERNEYT`. Это вероятная причина, по которой читающую половину
никогда и не подключили: показать её честно пока нечем.

**Что нужно (движку, не мне):** вернуть из `streakOf` длину серии в календарных днях — первый
день засчитанного прогона и число молчаливых дней внутри него. Тогда вид скажет честно
«3 отмечено за 107 дней» вместо «3 подряд», и гейт из докстринга станет исполнимым.
Я не трогаю ни `commitment-*`, ни их тесты (правило промпта 2: нашёл баг — скажи, не правь).

---

## 4f. Четыре калькулятора отвечают на один вопрос

Найдено при полном проходе, включая семь поверхностей за гейтом 3-го уровня, которых я до
этого не открывал ни разу. Это самое крупное нарушение правила «у факта одно место рождения»
из всех, и оно объясняет ощущение несвязности лучше любого отдельного шва.

На вопрос **«какая сфера сейчас без внимания»** приложение отвечает четырьмя способами,
каждый со своей арифметикой и своим окном:

| Где | Как считает | Окно |
|---|---|---|
| Прогресс, `balanceIndex()` | коэффициент вариации минут + охват сфер | скользящее |
| Прогресс, `sphereRhythmSummary()` | против объявленной частоты `targetPerWeek` | 14 дней |
| Питомцы, `petStats()` | «сытость»: XP/4 с потолком 25 и линейным спадом | 10 дней |
| Доска, `boardNeglectedSpheres()` | не было ни одного закрытого дела | 7 дней |

**Доказано вживую, а не выведено.** На одном экране Прогресса одновременно стояло:

> Без внимания: **Наука**
> Реже, чем ты решил: **Тело**

Одновременно Питомцы называли голодной Науку, а Доска не считала запущенной ни одну.
Четыре ответа, три разных.

⚠️ **Половину этого противоречия сделал я** — строка про ритм добавлена 02.09 и встала рядом
со старой, вместо неё. Исправлено сразу: на Прогрессе теперь одна строка. Побеждает
объявленная частота (она судит по тому, что человек сам решил, а не по выведенной ровности);
если частот не объявлено ни одной, остаётся прежняя строка ровно как была.

✅ **РЕШЕНО Альбертом 03.09.** Не «свести в один калькулятор», а **один факт и два
читателя**, каждый со своим прочтением:

- **Сытость сферы — единственный факт.** Он и есть ответ на «что запущено».
- **Прогресс** судит не по норме, выбранной за человека, а по частоте, выясненной
  **в разговоре**: агент либо спрашивает желаемую частоту напрямую, либо выводит её из целей,
  желаний и беспокойств человека — и задаёт уточняющий вопрос, когда нужно. Сегодня
  `targetPerWeek` это числовое поле в форме настроек (пустое = сфера не судится, выдуманной
  нормы нет). Направление: то же поле заполняется разговором, а не формой. ⚠️ Правило
  остаётся в силе и для агента: вывод разговора пишется **в то же поле**, а не в своё
  параллельное.
- **Питомцы** — не отдельный счётчик, а **отражение** сытости сферы, и у каждого питомца
  **свой аппетит**: один и тот же факт даёт разным питомцам разное состояние.
- **Доска** своего трекера не имеет вовсе. Выполненные заказы идут в опыт и в сытость
  соответствующих сфер.

⚠️ **Снимаю своё возражение, что это делает питомца «неживым».** Оно было неверным: питомец
с собственным аппетитом — это читатель факта со своим прочтением, а не датчик. Художественный
слой (анимация, реплики) читает состояние питомца, а состояние выводится из пары «сытость
сферы + аппетит этого питомца». Это ровно правило «у факта одно место рождения, читателей
много» — и питомец при этом получает то, чего у него нет сегодня: он голоден по причине,
которую человек может увидеть, и причина та же, что называет Прогресс.

**Было три производителя** — Прогресс, Питомцы, Доска. Свести их в один можно:
`sphere-frequency-v1` уже отдаёт статус каждой сферы (`under/ok/over/unset/paused`) и
`daysSinceTouch` для тех, у кого частота не объявлена, — то есть умеет ответить и без
выдуманной нормы. Но «сытость» питомца — это ещё и художественное состояние (анимация,
реплики), а приоритет доски меняет то, что человеку предлагают. Это уже не провод, а
продуктовое решение, поэтому оно **предложение, а не правка**.

---

## 4e. Семь имён у одной вещи

Самое сильное «не чувствуется одной нитью», что осталось, — не провод, а слова. Вопрос у
человека один: **что мне делать дальше.** Приложение отвечает на него семью способами:

| Где | Как названо |
|---|---|
| Форма создания цели | «Первый шаг» |
| Карточка цели | «Следующий шаг» |
| Связанные квесты цели | «Следующие действия» |
| Экран «Цели» | «Что важно сейчас» |
| Герой дня | «Следующий ход уже выбран» |
| Тень, заявленный ход | «Сейчас важнее всего» |
| Ядро дня | «Ядро дня» |

Часть из них — разные вещи и должны называться по-разному: «Ядро дня» (то, ради чего день
считается состоявшимся) и «ход» Тени (своевременное вмешательство) — не то же самое, что
следующий пункт списка.

Но три пары описывают **один и тот же объект** и различаются только экраном:

- `goal.steps[0]` — «Первый шаг» при создании и «Следующий шаг» в карточке. Одно поле, два
  имени.
- «Следующий шаг» и «Следующие действия» — на одном экране, про одно отношение.
- «Что важно сейчас» (Цели) и «Сейчас важнее всего» (Тень) — одно обещание, разные слова.

⚠️ **Не переписано.** Копирайт — решение Альберта (правило 7), а тон здесь несёт больше
смысла, чем разметка. Это предложение, а не правка: свести три пары к одному слову каждая.

---

## 5. Отдельно: мастер был красным

Не часть задания, но чинить пришлось до всего остального. `21f951b` восстановил шесть
тест-файлов в состояние **до v215** и разъехал релизные пины. Подробности и бисект —
в `DEVLOG.md`, запись `[2026-09-02] Тесты v215`.

---

## 6. Что осталось

Открытыми остаются **№2/№3** (Attention ↔ уговор ↔ Секретарь — брать одним движением) и
**№5** (Логово не следует из жизни). №2/№3 расписаны и закреплены за другими агентами
(`HANDOFF-CODEX-SECRETARY-UI.md`, producer-адаптеры в `BACKLOG.md`) — дублировать их работу
означало бы ровно то столкновение, ради которого писался протокол. №5 — продуктовый вопрос:
что должно питать связь с компаньоном, решает Альберт.

К ним добавились §4b и §4c. Оба требуют одного решения и не требуют кода: **где именно** эти
вещи должны показаться человеку. Пока ответа нет, три готовых модуля и четыре сохранённых
ответа продолжают лежать в темноте — второй месяц в случае `sphere-frequency` и
`arena-audit`.
