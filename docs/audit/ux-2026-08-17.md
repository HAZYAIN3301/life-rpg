# Аудит интерфейса — 2026-08-17

Найдено 167 мест в `public/app.js`. Скрипт: `scripts/audit-ux.mjs`.

| Класс | Мест |
|---|---|
| Текст мимо переводчика (немец увидит русский) | 1 |
| Кнопка может остаться выключенной после сбоя | 29 |
| Сообщение о сбое без способа повторить | 41 |
| Системные окна браузера вместо интерфейса | 11 |
| Пустой catch — сбой исчезает бесследно | 83 |
| Ответ сервера используется без проверки статуса | 2 |

## Текст мимо переводчика (немец увидит русский) — 1

> Точный класс: каждое место — реальный русский текст в нерусском интерфейсе.

- `app.js:21032` — confirm() с русским шаблоном без t()
  ```js
  confirm(`Восстановить «${name}» для ${user} из снимка ${stamp.slice(0, 19).replace('T', ' ')}?\nТекущее состояние сохран
  ```

## Кнопка может остаться выключенной после сбоя — 29

> ⚠️ ПЕРЕСТРАХОВЫВАЕТСЯ. Границы функции определяются грубо, вложенные стрелки ломают разбор. Проверять руками — часть мест включает кнопку в `finally` или в дальней ветке.

- `app.js:11282` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; });
  ```
- `app.js:19708` — кнопка выключается и до конца функции не включается обратно
  ```js
  if (errEl) errEl.setAttribute('role', 'alert'); if (btn) btn.disabled = true;
  ```
- `app.js:19722` — кнопка выключается и до конца функции не включается обратно
  ```js
  errEl.setAttribute('role', 'alert'); btn.disabled = true;
  ```
- `app.js:19740` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19756` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19784` — кнопка выключается и до конца функции не включается обратно
  ```js
  const btn = f.querySelector('button[type="submit"]'); btn.disabled = true;
  ```
- `app.js:19804` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19822` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19840` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19856` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19879` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19892` — кнопка выключается и до конца функции не включается обратно
  ```js
  const msg = f.querySelector('#pin-change-msg'), btn = f.querySelector('button[type="submit"]'); msg.setAttribute('role',
  ```
- `app.js:19921` — кнопка выключается и до конца функции не включается обратно
  ```js
  buttons.forEach((button) => { button.disabled = true; });
  ```
- `app.js:19950` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true; msg.style.color = 'var(--muted)'; msg.textContent = files.length ? 'Обрабатываю файлы…' : 'Отправля
  ```
- `app.js:19983` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; });
  ```
- `app.js:20005` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; });
  ```
- `app.js:20027` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; }); if (status) status.textContent = t('Сохраняю…');
  ```
- `app.js:20044` — кнопка выключается и до конца функции не включается обратно
  ```js
  const controls = Array.from(f.elements); controls.forEach((control) => { control.disabled = true; }); if (status) status
  ```
- `app.js:20054` — кнопка выключается и до конца функции не включается обратно
  ```js
  const controls = Array.from(f.elements); controls.forEach((control) => { control.disabled = true; }); if (status) status
  ```
- `app.js:20116` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; });
  ```
- `app.js:20146` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; }); if (status) status.textContent = t('Сохраняю…');
  ```
- `app.js:20187` — кнопка выключается и до конца функции не включается обратно
  ```js
  const submit = f.querySelector('button[type="submit"]'); if (submit) submit.disabled = true;
  ```
- `app.js:20468` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```
- `app.js:20477` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```
- `app.js:20554` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```
- `app.js:20937` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true; input.disabled = true;
  ```
- `app.js:20948` — кнопка выключается и до конца функции не включается обратно
  ```js
  const result = overlay.querySelector('.account-import-result'); el.disabled = true;
  ```
- `app.js:21019` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```
- `app.js:21311` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```

## Сообщение о сбое без способа повторить — 41

> ⚠️ Требует разборки: часть сообщений живёт в формах, где повтор — это просто нажать кнопку ещё раз.

- `app.js:3744` — сообщение о сбое без видимого способа повторить
  ```js
  } catch (e) { console.error('save', name, e); toast(t('⚠️ Не удалось сохранить')); return false; }
  ```
- `app.js:6302` — сообщение о сбое без видимого способа повторить
  ```js
  toast(t('Не удалось загрузить слой аватара'));
  ```
- `app.js:9885` — сообщение о сбое без видимого способа повторить
  ```js
  toast(t('Не удалось запустить выбранный голос — выбери другой в Настройках'));
  ```
- `app.js:10292` — сообщение о сбое без видимого способа повторить
  ```js
  toast(t('Не удалось запустить эмбиент в этом браузере'));
  ```
- `app.js:11166` — сообщение о сбое без видимого способа повторить
  ```js
  toast(t('Не удалось сохранить перенос'));
  ```
- `app.js:11302` — сообщение о сбое без видимого способа повторить
  ```js
  if (status) status.textContent = t('Не удалось сохранить перенос');
  ```
- `app.js:11979` — сообщение о сбое без видимого способа повторить
  ```js
  if (!acts.length) { toast(t('Не удалось разобрать файлы — нужен GPX или TCX с временем')); return; }
  ```
- `app.js:12477` — сообщение о сбое без видимого способа повторить
  ```js
  if (!r.ok || !d.proposals) { if (res) res.innerHTML = `<p class="muted">Не удалось: ${esc(d.detail || d.error || 'ошибка
  ```
- `app.js:12481` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { if (res) res.innerHTML = '<p class="muted">Сетевая ошибка.</p>'; }
  ```
- `app.js:17718` — сообщение о сбое без видимого способа повторить
  ```js
  toast(t('Не удалось сохранить выбор пути. Попробуй ещё раз.'));
  ```
- `app.js:17995` — сообщение о сбое без видимого способа повторить
  ```js
  catch { toast(t('Не удалось загрузить (нужен админ / нет юзера)')); return; }
  ```
- `app.js:18015` — сообщение о сбое без видимого способа повторить
  ```js
  try { const r = await fetch('/api/feedback'); if (!r.ok) throw 0; list = await r.json(); } catch { toast(t('Не удалось з
  ```
- `app.js:19713` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { if (errEl) errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19726` — сообщение о сбое без видимого способа повторить
  ```js
  else { errEl.textContent = accountError(data, 'Не удалось войти'); f.password.value = ''; }
  ```
- `app.js:19727` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19745` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19760` — сообщение о сбое без видимого способа повторить
  ```js
  else errEl.textContent = accountError(data, 'Не удалось сбросить');
  ```
- `app.js:19761` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19810` — сообщение о сбое без видимого способа повторить
  ```js
  else msg.textContent = accountError(data, 'Не удалось отправить');
  ```
- `app.js:19811` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19830` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19845` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); msg.style.color = 'var(--bad)'; }
  ```
- `app.js:19884` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); msg.setAttribute('role', 'alert'); }
  ```
- `app.js:19897` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); msg.style.color = 'var(--bad)'; }
  ```
- `app.js:19908` — сообщение о сбое без видимого способа повторить
  ```js
  .catch(() => { msg.textContent = t('Сетевая ошибка'); msg.style.color = '#e0526a'; });
  ```
- `app.js:19928` — сообщение о сбое без видимого способа повторить
  ```js
  msg.textContent = accountError(data, 'Не удалось изменить золото.'); msg.setAttribute('role', 'alert'); return;
  ```
- `app.js:19934` — сообщение о сбое без видимого способа повторить
  ```js
  msg.textContent = t('Сетевая ошибка'); msg.setAttribute('role', 'alert');
  ```
- `app.js:19968` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); msg.style.color = '#e0526a'; }
  ```
- `app.js:20012` — сообщение о сбое без видимого способа повторить
  ```js
  if (status) status.textContent = t('Не удалось сохранить квест');
  ```
- `app.js:20122` — сообщение о сбое без видимого способа повторить
  ```js
  if (status) status.textContent = t('Не удалось сохранить квест');
  ```
- `app.js:20470` — сообщение о сбое без видимого способа повторить
  ```js
  if (!response.ok) { toast(accountError(data, 'Сетевая ошибка')); return; }
  ```
- `app.js:20474` — сообщение о сбое без видимого способа повторить
  ```js
  }).catch(() => toast(t('Сетевая ошибка'))).finally(() => { if (el.isConnected) el.disabled = false; }); return;
  ```
- `app.js:20479` — сообщение о сбое без видимого способа повторить
  ```js
  if (!response.ok) { toast(accountError(data, 'Сетевая ошибка')); return; }
  ```
- `app.js:20483` — сообщение о сбое без видимого способа повторить
  ```js
  }).catch(() => toast(t('Сетевая ошибка'))).finally(() => { if (el.isConnected) el.disabled = false; }); return;
  ```
- `app.js:20793` — сообщение о сбое без видимого способа повторить
  ```js
  setAvatarCoreGender(el.dataset.gender).catch(() => toast(t('Не удалось загрузить аватар')));
  ```
- `app.js:21258` — сообщение о сбое без видимого способа повторить
  ```js
  if (!saved) { toast(t('Не удалось сохранить запись. Ничего не потеряно.')); return; }
  ```
- `app.js:21296` — сообщение о сбое без видимого способа повторить
  ```js
  if (!res.ok) { toast(res.error === 'limit' ? t('Три заказа сразу — уже список дел, а не приключение') : t('Не удалось вз
  ```
- `app.js:21318` — сообщение о сбое без видимого способа повторить
  ```js
  }, () => { if (el.isConnected) el.disabled = false; toast(t('Не удалось определить место')); }, { timeout: 10000, maximu
  ```
- `app.js:22430` — сообщение о сбое без видимого способа повторить
  ```js
  setTimeout(() => toast(sv === 'connected' ? '✓ Strava подключён — жми «Синхронизировать»' : 'Не удалось подключить Strav
  ```
- `app.js:22824` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { try { if (localSubscription) await localSubscription.unsubscribe(); } catch {} State.pushOn = false; toast(t('
  ```
- `app.js:22859` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { toast(t('Не удалось открыть установку. Попробуй из меню браузера.')); }
  ```

## Системные окна браузера вместо интерфейса — 11

- `app.js:20907` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(t('Отключить Strava? Уже импортированные тренировки останутся, новые синхронизироваться не будут.'))) retur
  ```
- `app.js:21032` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(`Восстановить «${name}» для ${user} из снимка ${stamp.slice(0, 19).replace('T', ' ')}?\nТекущее состояние с
  ```
- `app.js:21207` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(`⚔️ ${t('Клятва Кремню: завершить до конца дня')} — «${q.title}».\n${t('Сдержишь — золото за квест ×1.5. Пр
  ```
- `app.js:21214` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  const t = questById(id); if (t && t.done && !confirm(`${t('Удалить')} «${t.title}»?`)) return;
  ```
- `app.js:21241` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  const a = (State.antihabits || []).find((x) => x.id === id); if (!a || !confirm(`${t('Удалить')} «${a.title}»?`)) return
  ```
- `app.js:21537` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(t('Создать новую карту? Повреждённый файл будет заменён стартовыми деревьями.'))) return;
  ```
- `app.js:21591` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(`${t('Удалить узел')} «${treeNodeCopy(node, 'title')}»? ${t('Связи с ним будут сняты.')}`)) return;
  ```
- `app.js:21850` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (ep && confirm(`${t('Удалить эпизод')} «${ep.title}»? ${t('Опыт за него тоже уйдёт.')}`)) {
  ```
- `app.js:21965` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(msg)) return;
  ```
- `app.js:21966` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(t('Точно? Отменить это действие нельзя.'))) return;
  ```
- `app.js:22046` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(t('Удалить привычку? Её отметки и опыт тоже удалятся.'))) return;
  ```

## Пустой catch — сбой исчезает бесследно — 83

> ⚠️ Требует разборки: у декоративных вещей (звук, анимация, риг) пустой catch намеренный — сбой там не должен ломать рендер. Смотреть только те, где за catch стоит пользовательское действие.

- `app.js:3534` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:4115` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { localStorage.setItem('liferpg_seen_guide', '1'); } catch {}
  ```
- `app.js:4130` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (demoX7Requested()) try { localStorage.setItem('liferpg_seen_guide', '1'); } catch {}
  ```
- `app.js:4948` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { browserId = localStorage.getItem(storageKey) || ''; } catch {}
  ```
- `app.js:4957` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { localStorage.setItem(storageKey, browserId); } catch {}
  ```
- `app.js:5016` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:5253` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  setTimeout(() => { try { toast('⚔️ ' + t(FLINT_LINES.oathKept[Math.floor(Math.random() * FLINT_LINES.oathKept.length)]))
  ```
- `app.js:5260` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { const c = ensureCompanion(); c.bond += 2; c.lastSeen = todayStr(); Store.save('settings', State.settings); } catch
  ```
- `app.js:6278` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:6474` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { config.onExtract(); } catch {}
  ```
- `app.js:7370` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:7387` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (next) { setTimeout(() => { try { toast(`🚩 ${t('Похоже на веху')}: «${next.title}» — ${t('загляни в карту пути')}`);
  ```
- `app.js:7388` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:9384` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (dirty && State.phase === 'app') { try { Store.save('settings', State.settings); } catch {} }
  ```
- `app.js:9453` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:9467` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  setTimeout(() => { try { toast('🔥 ' + line); } catch {} }, 1600);
  ```
- `app.js:9469` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:9736` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  function ensureAudio() { try { if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)(); if (a
  ```
- `app.js:9844` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { speechSynthesis.cancel(); } catch {}
  ```
- `app.js:9979` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { speechSynthesis.addEventListener('voiceschanged', () => { ttsRefreshVoices(); ttsSyncVoiceSelect(); }); } catch {}
  ```
- `app.js:10059` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { _ambientMedia.pause(); } catch {}
  ```
- `app.js:10064` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  _ambientNodes.forEach((n) => { try { n.stop(); } catch {} try { n.disconnect(); } catch {} });
  ```
- `app.js:10173` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { source.disconnect(); filter.disconnect(); gain.disconnect(); if (panner) panner.disconnect(); } catch {}
  ```
- `app.js:10256` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  oscillator.onended = () => { try { oscillator.disconnect(); gain.disconnect(); if (panner) panner.disconnect(); } catch 
  ```
- `app.js:10314` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { new Notification(title, { body }); } catch {}
  ```
- `app.js:10381` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (pipWindow) { try { pipWindow.focus(); } catch {} return; }
  ```
- `app.js:10405` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  function closeFocusWidget() { if (pipWindow && !pipWindow.closed) { try { pipWindow.close(); } catch {} } pipWindow = nu
  ```
- `app.js:11977` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { const p = parseWorkoutFile(f.name, await f.text()); if (p) acts.push(p); } catch {}
  ```
- `app.js:12069` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let restGap = 0; try { restGap = restGapDays(); } catch {}
  ```
- `app.js:12070` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let quiet = 0; try { quiet = quietDaysBefore(); } catch {}
  ```
- `app.js:12190` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (_dayRec) { try { _dayRec.stop(); } catch {} _dayRec = null; if (btn) { btn.textContent = '🎤 Говорить'; btn.classLis
  ```
- `app.js:12195` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { rec.start(); _dayRec = rec; if (btn) { btn.textContent = '⏹ Стоп'; btn.classList.add('rec'); } } catch {}
  ```
- `app.js:12336` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (_epRec) { try { _epRec.stop(); } catch {} _epRec = null; if (btn) { btn.textContent = '🎤 ' + t('Говорить'); btn.cla
  ```
- `app.js:12341` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { rec.start(); _epRec = rec; if (btn) { btn.textContent = '⏹ ' + t('Стоп'); btn.classList.add('rec'); } } catch {}
  ```
- `app.js:12498` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { return JSON.parse(slice); } catch {}
  ```
- `app.js:13486` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { showMoment(kind); } catch {}
  ```
- `app.js:14361` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('coin'); } catch {}
  ```
- `app.js:16573` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { el.setPointerCapture(e.pointerId); } catch {}
  ```
- `app.js:16583` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { if (el.hasPointerCapture(e.pointerId)) el.releasePointerCapture(e.pointerId); } catch {}
  ```
- `app.js:17679` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (avatarMotionReduced()) overlay.querySelectorAll('video').forEach((video) => { try { video.pause(); } catch {} });
  ```
- `app.js:17723` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx(id === 'control' ? 'levelup' : 'achievement'); } catch {}
  ```
- `app.js:17728` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { publishLeaderboard(); } catch {}
  ```
- `app.js:19620` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:20253` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let ok = false; try { ok = !!d.when(); } catch {}
  ```
- `app.js:20263` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (d) { if (!t.seenDrips.includes(d.id)) t.seenDrips.push(d.id); if (d.disc) { try { markDiscovered(d.disc); } catch {}
  ```
- `app.js:20266` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (modal === 'path') { tutorialPaint(); try { showPathChoiceModal({ source: 'drip', returnFocus }); } catch {} return; 
  ```
- `app.js:20267` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (view) { State.view = view; try { markDiscovered(view); } catch {} render(); } else tutorialPaint();
  ```
- `app.js:20272` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  const repaint = () => { try { tutorialPaint(); } catch {} };
  ```
- `app.js:20276` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { new MutationObserver(repaint).observe(document.body, { childList: true }); } catch {}
  ```
- `app.js:20280` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  function tutorialFinish() { const tut = ensureTutorial(); tut.active = false; tut.done = true; Store.save('settings', St
  ```
- `app.js:20287` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20454` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { tutorialAdvance(action); } catch {}
  ```
- `app.js:20610` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20680` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx(['train', 'pushup', 'stretch', 'whistle'].includes(mode) ? 'complete' : 'click'); } catch {}
  ```
- `app.js:20698` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx(mode === 'greet' ? 'click' : 'complete'); } catch {}
  ```
- `app.js:20733` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx(mode === 'greet' ? 'click' : 'complete'); } catch {}
  ```
- `app.js:20745` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('click'); } catch {}
  ```
- `app.js:20801` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20824` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20866` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20873` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20880` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:21211` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('levelup'); } catch {}
  ```
- `app.js:21635` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { await navigator.share({ files: [file], title: 'Моя неделя в Satoru' }); } catch {}
  ```
- `app.js:21699` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:21701` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('achievement'); } catch {}
  ```
- `app.js:21817` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } else if (action === 'dayrec-close') { if (_dayRec) { try { _dayRec.stop(); } catch {} _dayRec = null; } const m = docu
  ```
- `app.js:21854` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } else if (action === 'ep-close') { if (_epRec) { try { _epRec.stop(); } catch {} _epRec = null; } const m = document.ge
  ```
- `app.js:21880` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } else if (action === 'install-dismiss') { try { localStorage.setItem('gojo_install_dismiss', '1'); } catch {} render();
  ```
- `app.js:22199` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let data = {}; try { data = await response.json(); } catch {}
  ```
- `app.js:22408` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:22420` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:22424` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:22433` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:22447` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  localStorage.setItem('liferpg_seen_guide', '1'); setTimeout(() => { try { tutorialStart(); } catch {} }, 600);
  ```
- `app.js:22462` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch (e) {}
  ```
- `app.js:22665` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { e.dataTransfer.setData('text/plain', _wkDragId); } catch {}
  ```
- `app.js:22705` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { e.dataTransfer.setData('text/plain', _calDragId); } catch {}
  ```
- `app.js:22752` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  _reminderTimers.push(setTimeout(() => { try { new Notification('🗓 ' + t.title, { body: 'Время по плану: ' + t.startTime
  ```
- `app.js:22820` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let tz = 'Europe/Berlin'; try { tz = Intl.DateTimeFormat().resolvedOptions().timeZone || tz; } catch {}
  ```
- `app.js:22824` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch { try { if (localSubscription) await localSubscription.unsubscribe(); } catch {} State.pushOn = false; toast(t('
  ```
- `app.js:22931` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { fetch('/api/data/settings', opt(State.settings)); fetch('/api/data/habits', opt(State.habits)); } catch {}
  ```
- `app.js:22951` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```

## Ответ сервера используется без проверки статуса — 2

> Проверять руками: часть мест ловит ошибку в теле ответа (`d.error`), а не по статусу.

- `app.js:12353` — ответ используется без проверки .ok/status
  ```js
  const r = await fetch('/api/ai/propose', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.s
  ```
- `app.js:16345` — ответ используется без проверки .ok/status
  ```js
  const r = await fetch('/api/ai/propose', { method: 'POST', headers: { 'Content-Type': 'application/json' },
  ```

