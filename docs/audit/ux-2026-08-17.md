# Аудит интерфейса — 2026-08-17

Найдено 226 мест в `public/app.js`. Скрипт: `scripts/audit-ux.mjs`.

| Класс | Мест |
|---|---|
| Текст мимо переводчика (немец увидит русский) | 60 |
| Кнопка может остаться выключенной после сбоя | 29 |
| Сообщение о сбое без способа повторить | 41 |
| Системные окна браузера вместо интерфейса | 11 |
| Пустой catch — сбой исчезает бесследно | 83 |
| Ответ сервера используется без проверки статуса | 2 |

## Текст мимо переводчика (немец увидит русский) — 60

> Точный класс: каждое место — реальный русский текст в нерусском интерфейсе.

- `app.js:3686` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('⚠️ Не удалось сохранить'
  ```
- `app.js:4102` — toast() с русским шаблоном без t()
  ```js
  toast(`📦 Программа «${prog.name}» добавлена`
  ```
- `app.js:6244` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Не удалось загрузить слой аватара'
  ```
- `app.js:7111` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Редактор вернул неверную схему образа'
  ```
- `app.js:7120` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Образ сохранён'
  ```
- `app.js:9827` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Не удалось запустить выбранный голос — выбери другой в Настройках'
  ```
- `app.js:10234` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Не удалось запустить эмбиент в этом браузере'
  ```
- `app.js:10322` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Плавающее окно недоступно в этом браузере — показываю плашку'
  ```
- `app.js:10376` — toast() с русским шаблоном без t()
  ```js
  toast(`⏱ ${fmtDur(t.actualMin)} записано в «${t.title}»`
  ```
- `app.js:11735` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('🤖 Лимит ИИ на месяц исчерпан — добавь свой ключ в Настройках'
  ```
- `app.js:12141` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Расскажи или впиши, что делал'
  ```
- `app.js:12185` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Ничего не выбрано'
  ```
- `app.js:12411` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Напиши текст'
  ```
- `app.js:12455` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Сначала опиши в поле выше — что за цели/уровни'
  ```
- `app.js:12458` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('📋 Промпт скопирован — вставь своему ИИ'
  ```
- `app.js:12475` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Вставь ответ ИИ'
  ```
- `app.js:12494` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Ничего не выбрано'
  ```
- `app.js:13035` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Браузер не поддерживает запись'
  ```
- `app.js:13038` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Нужен доступ к '
  ```
- `app.js:13059` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Пустая запись'
  ```
- `app.js:13064` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Не удалось сохранить запись'
  ```
- `app.js:13070` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Ошибка сохранения записи'
  ```
- `app.js:17935` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Не удалось загрузить (нужен админ / нет юзера)'
  ```
- `app.js:17955` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Не удалось загрузить (нужен админ)'
  ```
- `app.js:19610` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('✨ Имя сохранено'
  ```
- `app.js:19975` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('📝 В Заметках'
  ```
- `app.js:20034` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('🤖 Ключ сохранён'
  ```
- `app.js:20346` — toast() с русским шаблоном без t()
  ```js
  toast(`🔒 «${s.label}» откроется на ур.${s.gate}`
  ```
- `app.js:20362` — toast() с русским шаблоном без t()
  ```js
  toast(`Время ${fmtHM(min)} подставлено в форму`
  ```
- `app.js:20733` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Не удалось загрузить аватар'
  ```
- `app.js:20828` — toast() с русским шаблоном без t()
  ```js
  toast(`🔒 Откроется на ур.${it.lvl}`
  ```
- `app.js:20847` — confirm() с русским литералом — не пройдёт через t()
  ```js
  confirm('Отключить Strava? Уже импортированные тренировки останутся, новые синхронизироваться не будут.'
  ```
- `app.js:20848` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Strava отключён'
  ```
- `app.js:20958` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Нет снимка'
  ```
- `app.js:20959` — confirm() с русским шаблоном без t()
  ```js
  confirm(`Восстановить «${name}» для ${user} из снимка ${stamp.slice(0, 19).replace('T', ' ')}?\nТекущее состояние сохран
  ```
- `app.js:20964` — toast() с русским шаблоном без t()
  ```js
  toast(`✓ Восстановлено: ${name}`
  ```
- `app.js:20965` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Ошибка сети'
  ```
- `app.js:20985` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Ошибка доступа'
  ```
- `app.js:20987` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Репортов пока нет'
  ```
- `app.js:20996` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('📋 Скопировано! Вставь в чат с Claude.'
  ```
- `app.js:20997` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Ошибка: '
  ```
- `app.js:21067` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Ошибка сети'
  ```
- `app.js:21134` — confirm() с русским шаблоном без t()
  ```js
  confirm(`⚔️ Клятва Кремню: завершить «${q.title}» до конца дня.\nСдержишь — золото за квест ×1.5. Провалишь — сгорит ${C
  ```
- `app.js:21141` — confirm() с русским шаблоном без t()
  ```js
  confirm(`Удалить «${t.title}»?`
  ```
- `app.js:21148` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('🤸 Разминка в плане — тело скажет спасибо'
  ```
- `app.js:21153` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('🧘 Мобилка в плане — спина и плечи скажут спасибо'
  ```
- `app.js:21156` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Подсказки мобилки отключены'
  ```
- `app.js:21180` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Перенесено на сегодня'
  ```
- `app.js:21801` — toast() с русским шаблоном без t()
  ```js
  toast(`🎁 Награда пати: +${d.reward.gold} 🪙 · +${d.reward.boostPct}% XP на ${d.reward.boostHours}ч`
  ```
- `app.js:21804` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Сетевая ошибка'
  ```
- `app.js:21847` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Нет квестов со временем — поставь их в Календаре'
  ```
- `app.js:21848` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('📆 .ics скачан — открой его в Календаре для импорта'
  ```
- `app.js:21959` — toast() с русским шаблоном без t()
  ```js
  toast(`🏁 «${sk.name}» убрана с колеса — это проект. Веди её в Целях.`
  ```
- `app.js:21973` — confirm() с русским литералом — не пройдёт через t()
  ```js
  confirm('Удалить привычку? Её отметки и опыт тоже удалятся.'
  ```
- `app.js:22525` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('🤖 ИИ по умолчанию: '
  ```
- `app.js:22534` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Нужна хотя бы одна категория'
  ```
- `app.js:22684` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Браузер не поддерживает уведомления'
  ```
- `app.js:22685` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('🔕 Напоминания выключены'
  ```
- `app.js:22686` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('🔔 Напоминания включены · работают пока вкладка открыта'
  ```
- `app.js:22688` — toast() с русским литералом — не пройдёт через t()
  ```js
  toast('Нужно разрешить уведомления в браузере'
  ```

## Кнопка может остаться выключенной после сбоя — 29

> ⚠️ ПЕРЕСТРАХОВЫВАЕТСЯ. Границы функции определяются грубо, вложенные стрелки ломают разбор. Проверять руками — часть мест включает кнопку в `finally` или в дальней ветке.

- `app.js:11224` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; });
  ```
- `app.js:19648` — кнопка выключается и до конца функции не включается обратно
  ```js
  if (errEl) errEl.setAttribute('role', 'alert'); if (btn) btn.disabled = true;
  ```
- `app.js:19662` — кнопка выключается и до конца функции не включается обратно
  ```js
  errEl.setAttribute('role', 'alert'); btn.disabled = true;
  ```
- `app.js:19680` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19696` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19724` — кнопка выключается и до конца функции не включается обратно
  ```js
  const btn = f.querySelector('button[type="submit"]'); btn.disabled = true;
  ```
- `app.js:19744` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19762` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19780` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19796` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19819` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true;
  ```
- `app.js:19832` — кнопка выключается и до конца функции не включается обратно
  ```js
  const msg = f.querySelector('#pin-change-msg'), btn = f.querySelector('button[type="submit"]'); msg.setAttribute('role',
  ```
- `app.js:19861` — кнопка выключается и до конца функции не включается обратно
  ```js
  buttons.forEach((button) => { button.disabled = true; });
  ```
- `app.js:19890` — кнопка выключается и до конца функции не включается обратно
  ```js
  btn.disabled = true; msg.style.color = 'var(--muted)'; msg.textContent = files.length ? 'Обрабатываю файлы…' : 'Отправля
  ```
- `app.js:19923` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; });
  ```
- `app.js:19945` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; });
  ```
- `app.js:19967` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; }); if (status) status.textContent = t('Сохраняю…');
  ```
- `app.js:19984` — кнопка выключается и до конца функции не включается обратно
  ```js
  const controls = Array.from(f.elements); controls.forEach((control) => { control.disabled = true; }); if (status) status
  ```
- `app.js:19994` — кнопка выключается и до конца функции не включается обратно
  ```js
  const controls = Array.from(f.elements); controls.forEach((control) => { control.disabled = true; }); if (status) status
  ```
- `app.js:20056` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; });
  ```
- `app.js:20086` — кнопка выключается и до конца функции не включается обратно
  ```js
  controls.forEach((control) => { control.disabled = true; }); if (status) status.textContent = t('Сохраняю…');
  ```
- `app.js:20127` — кнопка выключается и до конца функции не включается обратно
  ```js
  const submit = f.querySelector('button[type="submit"]'); if (submit) submit.disabled = true;
  ```
- `app.js:20408` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```
- `app.js:20417` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```
- `app.js:20494` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```
- `app.js:20877` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true; input.disabled = true;
  ```
- `app.js:20888` — кнопка выключается и до конца функции не включается обратно
  ```js
  const result = overlay.querySelector('.account-import-result'); el.disabled = true;
  ```
- `app.js:20947` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```
- `app.js:21238` — кнопка выключается и до конца функции не включается обратно
  ```js
  el.disabled = true;
  ```

## Сообщение о сбое без способа повторить — 41

> ⚠️ Требует разборки: часть сообщений живёт в формах, где повтор — это просто нажать кнопку ещё раз.

- `app.js:3686` — сообщение о сбое без видимого способа повторить
  ```js
  } catch (e) { console.error('save', name, e); toast('⚠️ Не удалось сохранить'); return false; }
  ```
- `app.js:6244` — сообщение о сбое без видимого способа повторить
  ```js
  toast('Не удалось загрузить слой аватара');
  ```
- `app.js:9827` — сообщение о сбое без видимого способа повторить
  ```js
  toast('Не удалось запустить выбранный голос — выбери другой в Настройках');
  ```
- `app.js:10234` — сообщение о сбое без видимого способа повторить
  ```js
  toast('Не удалось запустить эмбиент в этом браузере');
  ```
- `app.js:11108` — сообщение о сбое без видимого способа повторить
  ```js
  toast(t('Не удалось сохранить перенос'));
  ```
- `app.js:11244` — сообщение о сбое без видимого способа повторить
  ```js
  if (status) status.textContent = t('Не удалось сохранить перенос');
  ```
- `app.js:11921` — сообщение о сбое без видимого способа повторить
  ```js
  if (!acts.length) { toast(t('Не удалось разобрать файлы — нужен GPX или TCX с временем')); return; }
  ```
- `app.js:12419` — сообщение о сбое без видимого способа повторить
  ```js
  if (!r.ok || !d.proposals) { if (res) res.innerHTML = `<p class="muted">Не удалось: ${esc(d.detail || d.error || 'ошибка
  ```
- `app.js:12423` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { if (res) res.innerHTML = '<p class="muted">Сетевая ошибка.</p>'; }
  ```
- `app.js:17660` — сообщение о сбое без видимого способа повторить
  ```js
  toast(t('Не удалось сохранить выбор пути. Попробуй ещё раз.'));
  ```
- `app.js:17935` — сообщение о сбое без видимого способа повторить
  ```js
  catch { toast('Не удалось загрузить (нужен админ / нет юзера)'); return; }
  ```
- `app.js:17955` — сообщение о сбое без видимого способа повторить
  ```js
  try { const r = await fetch('/api/feedback'); if (!r.ok) throw 0; list = await r.json(); } catch { toast('Не удалось заг
  ```
- `app.js:19653` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { if (errEl) errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19666` — сообщение о сбое без видимого способа повторить
  ```js
  else { errEl.textContent = accountError(data, 'Не удалось войти'); f.password.value = ''; }
  ```
- `app.js:19667` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19685` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19700` — сообщение о сбое без видимого способа повторить
  ```js
  else errEl.textContent = accountError(data, 'Не удалось сбросить');
  ```
- `app.js:19701` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19750` — сообщение о сбое без видимого способа повторить
  ```js
  else msg.textContent = accountError(data, 'Не удалось отправить');
  ```
- `app.js:19751` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19770` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { errEl.textContent = t('Сетевая ошибка'); }
  ```
- `app.js:19785` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); msg.style.color = 'var(--bad)'; }
  ```
- `app.js:19824` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); msg.setAttribute('role', 'alert'); }
  ```
- `app.js:19837` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); msg.style.color = 'var(--bad)'; }
  ```
- `app.js:19848` — сообщение о сбое без видимого способа повторить
  ```js
  .catch(() => { msg.textContent = t('Сетевая ошибка'); msg.style.color = '#e0526a'; });
  ```
- `app.js:19868` — сообщение о сбое без видимого способа повторить
  ```js
  msg.textContent = accountError(data, 'Не удалось изменить золото.'); msg.setAttribute('role', 'alert'); return;
  ```
- `app.js:19874` — сообщение о сбое без видимого способа повторить
  ```js
  msg.textContent = t('Сетевая ошибка'); msg.setAttribute('role', 'alert');
  ```
- `app.js:19908` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { msg.textContent = t('Сетевая ошибка'); msg.style.color = '#e0526a'; }
  ```
- `app.js:19952` — сообщение о сбое без видимого способа повторить
  ```js
  if (status) status.textContent = t('Не удалось сохранить квест');
  ```
- `app.js:20062` — сообщение о сбое без видимого способа повторить
  ```js
  if (status) status.textContent = t('Не удалось сохранить квест');
  ```
- `app.js:20410` — сообщение о сбое без видимого способа повторить
  ```js
  if (!response.ok) { toast(accountError(data, 'Сетевая ошибка')); return; }
  ```
- `app.js:20414` — сообщение о сбое без видимого способа повторить
  ```js
  }).catch(() => toast(t('Сетевая ошибка'))).finally(() => { if (el.isConnected) el.disabled = false; }); return;
  ```
- `app.js:20419` — сообщение о сбое без видимого способа повторить
  ```js
  if (!response.ok) { toast(accountError(data, 'Сетевая ошибка')); return; }
  ```
- `app.js:20423` — сообщение о сбое без видимого способа повторить
  ```js
  }).catch(() => toast(t('Сетевая ошибка'))).finally(() => { if (el.isConnected) el.disabled = false; }); return;
  ```
- `app.js:20733` — сообщение о сбое без видимого способа повторить
  ```js
  setAvatarCoreGender(el.dataset.gender).catch(() => toast('Не удалось загрузить аватар'));
  ```
- `app.js:21185` — сообщение о сбое без видимого способа повторить
  ```js
  if (!saved) { toast(t('Не удалось сохранить запись. Ничего не потеряно.')); return; }
  ```
- `app.js:21223` — сообщение о сбое без видимого способа повторить
  ```js
  if (!res.ok) { toast(res.error === 'limit' ? t('Три заказа сразу — уже список дел, а не приключение') : t('Не удалось вз
  ```
- `app.js:21245` — сообщение о сбое без видимого способа повторить
  ```js
  }, () => { if (el.isConnected) el.disabled = false; toast(t('Не удалось определить место')); }, { timeout: 10000, maximu
  ```
- `app.js:22357` — сообщение о сбое без видимого способа повторить
  ```js
  setTimeout(() => toast(sv === 'connected' ? '✓ Strava подключён — жми «Синхронизировать»' : 'Не удалось подключить Strav
  ```
- `app.js:22751` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { try { if (localSubscription) await localSubscription.unsubscribe(); } catch {} State.pushOn = false; toast(t('
  ```
- `app.js:22786` — сообщение о сбое без видимого способа повторить
  ```js
  } catch { toast(t('Не удалось открыть установку. Попробуй из меню браузера.')); }
  ```

## Системные окна браузера вместо интерфейса — 11

- `app.js:20847` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm('Отключить Strava? Уже импортированные тренировки останутся, новые синхронизироваться не будут.')) return;
  ```
- `app.js:20959` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(`Восстановить «${name}» для ${user} из снимка ${stamp.slice(0, 19).replace('T', ' ')}?\nТекущее состояние с
  ```
- `app.js:21134` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(`⚔️ Клятва Кремню: завершить «${q.title}» до конца дня.\nСдержишь — золото за квест ×1.5. Провалишь — сгори
  ```
- `app.js:21141` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  const t = questById(id); if (t && t.done && !confirm(`Удалить «${t.title}»?`)) return;
  ```
- `app.js:21168` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  const a = (State.antihabits || []).find((x) => x.id === id); if (!a || !confirm(`${t('Удалить')} «${a.title}»?`)) return
  ```
- `app.js:21464` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(t('Создать новую карту? Повреждённый файл будет заменён стартовыми деревьями.'))) return;
  ```
- `app.js:21518` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(`${t('Удалить узел')} «${treeNodeCopy(node, 'title')}»? ${t('Связи с ним будут сняты.')}`)) return;
  ```
- `app.js:21777` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (ep && confirm(`${t('Удалить эпизод')} «${ep.title}»? ${t('Опыт за него тоже уйдёт.')}`)) {
  ```
- `app.js:21892` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(msg)) return;
  ```
- `app.js:21893` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm(t('Точно? Отменить это действие нельзя.'))) return;
  ```
- `app.js:21973` — системное окно браузера: не оформлено, не переводится, на телефоне выглядит чужим
  ```js
  if (!confirm('Удалить привычку? Её отметки и опыт тоже удалятся.')) return;
  ```

## Пустой catch — сбой исчезает бесследно — 83

> ⚠️ Требует разборки: у декоративных вещей (звук, анимация, риг) пустой catch намеренный — сбой там не должен ломать рендер. Смотреть только те, где за catch стоит пользовательское действие.

- `app.js:3476` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:4057` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { localStorage.setItem('liferpg_seen_guide', '1'); } catch {}
  ```
- `app.js:4072` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (demoX7Requested()) try { localStorage.setItem('liferpg_seen_guide', '1'); } catch {}
  ```
- `app.js:4890` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { browserId = localStorage.getItem(storageKey) || ''; } catch {}
  ```
- `app.js:4899` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { localStorage.setItem(storageKey, browserId); } catch {}
  ```
- `app.js:4958` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:5195` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  setTimeout(() => { try { toast('⚔️ ' + t(FLINT_LINES.oathKept[Math.floor(Math.random() * FLINT_LINES.oathKept.length)]))
  ```
- `app.js:5202` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { const c = ensureCompanion(); c.bond += 2; c.lastSeen = todayStr(); Store.save('settings', State.settings); } catch
  ```
- `app.js:6220` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:6416` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { config.onExtract(); } catch {}
  ```
- `app.js:7312` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:7329` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (next) { setTimeout(() => { try { toast(`🚩 ${t('Похоже на веху')}: «${next.title}» — ${t('загляни в карту пути')}`);
  ```
- `app.js:7330` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:9326` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (dirty && State.phase === 'app') { try { Store.save('settings', State.settings); } catch {} }
  ```
- `app.js:9395` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:9409` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  setTimeout(() => { try { toast('🔥 ' + line); } catch {} }, 1600);
  ```
- `app.js:9411` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:9678` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  function ensureAudio() { try { if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)(); if (a
  ```
- `app.js:9786` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { speechSynthesis.cancel(); } catch {}
  ```
- `app.js:9921` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { speechSynthesis.addEventListener('voiceschanged', () => { ttsRefreshVoices(); ttsSyncVoiceSelect(); }); } catch {}
  ```
- `app.js:10001` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { _ambientMedia.pause(); } catch {}
  ```
- `app.js:10006` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  _ambientNodes.forEach((n) => { try { n.stop(); } catch {} try { n.disconnect(); } catch {} });
  ```
- `app.js:10115` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { source.disconnect(); filter.disconnect(); gain.disconnect(); if (panner) panner.disconnect(); } catch {}
  ```
- `app.js:10198` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  oscillator.onended = () => { try { oscillator.disconnect(); gain.disconnect(); if (panner) panner.disconnect(); } catch 
  ```
- `app.js:10256` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { new Notification(title, { body }); } catch {}
  ```
- `app.js:10323` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (pipWindow) { try { pipWindow.focus(); } catch {} return; }
  ```
- `app.js:10347` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  function closeFocusWidget() { if (pipWindow && !pipWindow.closed) { try { pipWindow.close(); } catch {} } pipWindow = nu
  ```
- `app.js:11919` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { const p = parseWorkoutFile(f.name, await f.text()); if (p) acts.push(p); } catch {}
  ```
- `app.js:12011` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let restGap = 0; try { restGap = restGapDays(); } catch {}
  ```
- `app.js:12012` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let quiet = 0; try { quiet = quietDaysBefore(); } catch {}
  ```
- `app.js:12132` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (_dayRec) { try { _dayRec.stop(); } catch {} _dayRec = null; if (btn) { btn.textContent = '🎤 Говорить'; btn.classLis
  ```
- `app.js:12137` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { rec.start(); _dayRec = rec; if (btn) { btn.textContent = '⏹ Стоп'; btn.classList.add('rec'); } } catch {}
  ```
- `app.js:12278` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (_epRec) { try { _epRec.stop(); } catch {} _epRec = null; if (btn) { btn.textContent = '🎤 ' + t('Говорить'); btn.cla
  ```
- `app.js:12283` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { rec.start(); _epRec = rec; if (btn) { btn.textContent = '⏹ ' + t('Стоп'); btn.classList.add('rec'); } } catch {}
  ```
- `app.js:12440` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { return JSON.parse(slice); } catch {}
  ```
- `app.js:13428` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { showMoment(kind); } catch {}
  ```
- `app.js:14303` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('coin'); } catch {}
  ```
- `app.js:16515` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { el.setPointerCapture(e.pointerId); } catch {}
  ```
- `app.js:16525` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { if (el.hasPointerCapture(e.pointerId)) el.releasePointerCapture(e.pointerId); } catch {}
  ```
- `app.js:17621` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (avatarMotionReduced()) overlay.querySelectorAll('video').forEach((video) => { try { video.pause(); } catch {} });
  ```
- `app.js:17665` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx(id === 'control' ? 'levelup' : 'achievement'); } catch {}
  ```
- `app.js:17670` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { publishLeaderboard(); } catch {}
  ```
- `app.js:19560` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:20193` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let ok = false; try { ok = !!d.when(); } catch {}
  ```
- `app.js:20203` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (d) { if (!t.seenDrips.includes(d.id)) t.seenDrips.push(d.id); if (d.disc) { try { markDiscovered(d.disc); } catch {}
  ```
- `app.js:20206` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (modal === 'path') { tutorialPaint(); try { showPathChoiceModal({ source: 'drip', returnFocus }); } catch {} return; 
  ```
- `app.js:20207` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  if (view) { State.view = view; try { markDiscovered(view); } catch {} render(); } else tutorialPaint();
  ```
- `app.js:20212` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  const repaint = () => { try { tutorialPaint(); } catch {} };
  ```
- `app.js:20216` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { new MutationObserver(repaint).observe(document.body, { childList: true }); } catch {}
  ```
- `app.js:20220` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  function tutorialFinish() { const tut = ensureTutorial(); tut.active = false; tut.done = true; Store.save('settings', St
  ```
- `app.js:20227` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20394` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { tutorialAdvance(action); } catch {}
  ```
- `app.js:20550` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20620` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx(['train', 'pushup', 'stretch', 'whistle'].includes(mode) ? 'complete' : 'click'); } catch {}
  ```
- `app.js:20638` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx(mode === 'greet' ? 'click' : 'complete'); } catch {}
  ```
- `app.js:20673` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx(mode === 'greet' ? 'click' : 'complete'); } catch {}
  ```
- `app.js:20685` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('click'); } catch {}
  ```
- `app.js:20741` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20764` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20806` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20813` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:20820` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('complete'); } catch {}
  ```
- `app.js:21138` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('levelup'); } catch {}
  ```
- `app.js:21562` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { await navigator.share({ files: [file], title: 'Моя неделя в Satoru' }); } catch {}
  ```
- `app.js:21626` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:21628` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { sfx('achievement'); } catch {}
  ```
- `app.js:21744` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } else if (action === 'dayrec-close') { if (_dayRec) { try { _dayRec.stop(); } catch {} _dayRec = null; } const m = docu
  ```
- `app.js:21781` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } else if (action === 'ep-close') { if (_epRec) { try { _epRec.stop(); } catch {} _epRec = null; } const m = document.ge
  ```
- `app.js:21807` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } else if (action === 'install-dismiss') { try { localStorage.setItem('gojo_install_dismiss', '1'); } catch {} render();
  ```
- `app.js:22126` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let data = {}; try { data = await response.json(); } catch {}
  ```
- `app.js:22335` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:22347` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:22351` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:22360` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```
- `app.js:22374` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  localStorage.setItem('liferpg_seen_guide', '1'); setTimeout(() => { try { tutorialStart(); } catch {} }, 600);
  ```
- `app.js:22389` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch (e) {}
  ```
- `app.js:22592` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { e.dataTransfer.setData('text/plain', _wkDragId); } catch {}
  ```
- `app.js:22632` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { e.dataTransfer.setData('text/plain', _calDragId); } catch {}
  ```
- `app.js:22679` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  _reminderTimers.push(setTimeout(() => { try { new Notification('🗓 ' + t.title, { body: 'Время по плану: ' + t.startTime
  ```
- `app.js:22747` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  let tz = 'Europe/Berlin'; try { tz = Intl.DateTimeFormat().resolvedOptions().timeZone || tz; } catch {}
  ```
- `app.js:22751` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch { try { if (localSubscription) await localSubscription.unsubscribe(); } catch {} State.pushOn = false; toast(t('
  ```
- `app.js:22858` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  try { fetch('/api/data/settings', opt(State.settings)); fetch('/api/data/habits', opt(State.habits)); } catch {}
  ```
- `app.js:22878` — пустой catch: сбой не виден ни пользователю, ни в консоли
  ```js
  } catch {}
  ```

## Ответ сервера используется без проверки статуса — 2

> Проверять руками: часть мест ловит ошибку в теле ответа (`d.error`), а не по статусу.

- `app.js:12295` — ответ используется без проверки .ok/status
  ```js
  const r = await fetch('/api/ai/propose', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.s
  ```
- `app.js:16287` — ответ используется без проверки .ok/status
  ```js
  const r = await fetch('/api/ai/propose', { method: 'POST', headers: { 'Content-Type': 'application/json' },
  ```

