[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m piper.http_server -m ru_RU-irina-medium --data-dir /opt/piper/voices --host 0.0.0.0 --port 5000"
healthcheckPath = "/info"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
