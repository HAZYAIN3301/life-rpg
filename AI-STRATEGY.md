# Satoru — ИИ-стратегия перед запуском

> 2026-07-18. По тезису Альберта из партии фидбека: «поставить больший акцент на ИИ перед запуском». Это моя оценка тезиса + конкретный план. Читается вместе с `NEW-FEEDBACK-2026-07.md` (партия 4) и `LAUNCH-ALPHA.md`.

## Короткий ответ: тезис верный, но «больше ИИ» ≠ «больше ИИ-фич»

Я читал весь фидбек не как список пожеланий, а как один симптом, описанный с разных сторон. Симптом:

> **Модель тебя внутри приложения расходится с реальностью — и ты это чувствуешь.**

- `#6`: «показывает, что всё прекрасно и всё растёт… хотя я утомлён». Приложение говорит «баланс», ты — «вымотан».
- `#6`: рефлексия ценна, но «этот формат её не поддерживает» — она утекает и ни на что не влияет.
- `#6`: хочу «секретаря для человека, стремящегося к развитию».
- `#3`: помогать с *актуальной* проблемой (задачи без дедлайнов → прокрастинация; плохой сон → дереализация).
- `#9`: устал так, что «префронталка отключается» — нужен навигатор в этот момент, а не трекер.
- `#1`: «Джарвис». Это не фича — это как ощущается связный ИИ-слой, когда он есть.

Вывод: **ИИ — единственное, что может закрыть этот разрыв.** Логгер не умеет заметить, что ты устал, что рефлексия важна, что сегодня день не про продуктивность. ИИ умеет. Поэтому «больше акцента на ИИ» — правильно. Но не как «добавить ещё чат», а как **собрать разрозненные ИИ-куски в один цикл и закрыть две дыры в нём.**

## Цикл ИИ-слоя: Захват → Понимание → Совет → Действие

Это то, что превращает планер в секретаря. У Satoru уже есть три из четырёх кирпичей — наполовину.

| Кирпич | Что уже есть | Дыра |
|---|---|---|
| **Захват** (что сделал + что чувствуешь) | «Итог дня» ловит дела | ❌ выбрасывает рефлексию (`#6`) |
| **Понимание** (твоё реальное состояние) | индекс баланса, энергия | ❌ ложный баланс: отдых спрятан в «Здоровье» → зелёный, когда ты красный (`#6`) |
| **Совет** (один шаг под твою боль) | движок «Предложений», чат, «Заход» | ✅ закрыто 26.07 (Джарвис-1): чат получает `stateNowContext()` + недельный контекст + роль секретаря; предложения — guarded-фон нагрузки. DEVLOG 26.07 |
| **Действие** (совет → квесты) | квесты, «Заход», «Личная карта» вех | ✅ готово |

«Джарвис» из `#1` — это не пятый кирпич. Это **свойство собранного цикла**: когда захват не теряет рефлексию, понимание честное, а совет опирается на состояние — оно и ощущается как ассистент, который тебя знает.

## Решение перед запуском: ДВА кирпича, не пять

### 🥇 Кирпич 1 (ИИ) — Захват рефлексии + недельное зеркало

Ровно то, что ты протестировал и попросил (`#6`). «Итог дня» расширяется: помимо дел ИИ вытягивает **инсайты/рефлексию** («уперся в потолок финансирования», «плохо сплю») в отдельный журнал. Раз в неделю ИИ-разбор читает журнал + данные и **отражает твоё настоящее состояние + один шаг**: «ты много сделал, но не отдыхал 6 дней; сон пустой; 8 задач без дедлайна». Это разница между логгером и секретарём — та самая, которую ты хочешь.

> **Проверка кода (18.07): «зеркало» на 80% уже построено.** Фича `🤖 Разбор недели` в Статистике: `buildWeekContext()` собирает время по сферам, индекс баланса, радар, анти-привычки, **энергию** и шлёт «заботливому научному наставнику» через `/api/ai/analyze`. То есть вехикл готов. Реально не хватает ровно двух вещей, и обе — это дыры из таблицы цикла:
> 1. **Рефлексии в контексте нет** — `buildWeekContext` не видит журнал, потому что рефлексия нигде не сохраняется (дыра Захвата).
> 2. **«Дней без отдыха» не вычислимо** — потому что отдых не отдельная сфера (дыра Понимания = Кирпич 2).
>
> Значит Кирпич 1 = (а) захват рефлексии в «Итоге дня» → журнал, (б) добавить журнал + разрыв отдыха в `buildWeekContext`, (в) сместить системный промпт с «правды о времени» на «как ты на самом деле». UI зеркала трогать не надо. Дёшево.
>
> **✅ Реализовано (24.07), (а) оказалась ещё дешевле плана.** Проверка кода перед стартом показала: захват уже есть — `day.reflection` («Итог дня») и `week.intention`/`week.review` («Итоги недели») давно пишутся и хранятся, просто раньше уходили только в список на глазах юзера («Рефлексии этой недели»), а не в ИИ-контекст. Значит (а) не нужно строить с нуля — нужно было только (б) и (в): `buildWeekContext()` теперь читает `State.days[*].reflection` за 7 дней окна + текущие `intention`/`review`, системный промпт `runWeeklyReview()` явно ставит эти слова юзера ВАЖНЕЕ любых цифр и просит отвечать на конкретную боль из них, а не общими фразами. Копирайт карточки «Правда о времени и балансе» → «Как ты на самом деле, а не только цифры» (+ попутный фикс: старая композитная строка с `aiSourceHint()` технически не могла перевестись на DE/EN/UK/ES целиком — `t()` внутри `aiSourceHint()` резолвится eagerly в момент сборки строки, а префикс полагался на поздний DOM-pass; теперь префикс тоже через `t()`, оба резолвятся одинаково). Живая проверка в превью: `buildWeekContext()` с реальными данными отдаёт все три блока корректно, пусто — блоки корректно исчезают, карточка на RU/EN/DE переведена полностью. DEVLOG 2026-07-24.

### 🥈 Кирпич 2 (НЕ ИИ, но обязателен рядом) — Сфера «Отдых» + честное состояние

ИИ-осознание состояния бесполезно, если модель под ним врёт. Пока отдых = XP в «Здоровье», «ты не отдыхал 6 дней» даже **не вычислимо**. Нужно: отдых — отдельная (пресетная, для всех) сфера ИЛИ энергия/баланс считают отдых отдельно. Строка `Отдых / Восстановление` в пресетах уже есть — надо довести до структурной честности. Плюс ин-апп-объяснение энергии (капается до `maxCeil`, не бесконечно — `#6` спрашивал).

> **✅ Реализовано (24.07), дешевле плана.** Не завели отдельную обязательную сферу — это потребовало бы трогать онбординг, а для существующих юзеров новая пресетная сфера не появляется задним числом. Вместо этого переиспользован уже существующий `CANON_DOMAINS`-regex (`canonById('rest').re`), но применён к **названиям задач/привычек за день** (`dayHadRest`/`restGapDays`), а не к названиям сфер — так тренировка и отдых различимы, даже если оба живут в «Здоровье». `buildWeekContext()` получил строку честного состояния + объяснение потолка энергии; `runWeeklyReview()` знает, что разрыв ≥4 дня важнее % баланса; на «Сегодня» — мягкий нудж при разрыве ≥4 дня (после 5+ залогированных дел). Подробности и живая проверка — DEVLOG 2026-07-24.

**Всё. Два кирпича. Остальное — после запуска.**

## Жёсткий гейт: деньги

ИИ стоит за вызов. Альфа — **бесплатная, для незнакомцев**. Значит:

- Дом-ключ (наш счёт) остаётся **только для Pro**. Незнакомцы = BYOK или бесплатные Gemini/Groq.
- **ИИ-слой обязан деградировать без ключа**, а не ломаться. Захват рефлексии без ключа = просто сохранить текст (ценность есть даже без разбора). Зеркало без ключа = локальная сводка по цифрам (без прозы). Так уже сделано с авто-категорией.
- Тезис «больше ИИ» **не должен** означать «ИИ включён у всех по умолчанию» — это денежная бомба на альфе.

## Ловушки — назвать вслух

1. **«Джарвис» в лоб** (`#1`) — расфокус. Строим цикл, ассистент возникает сам. Не начинаем с «универсального помощника».
2. **B2B-пивот** (`#2`,`#10`,`#11`) — другой продукт, другая ЦА, после запуска на людях. Реальный, но не сейчас.
3. **ИИ как затычка для сломанной модели** — если под ИИ баланс врёт, ИИ будет уверенно врать красиво. Поэтому Кирпич 2 идёт вместе с Кирпичом 1, не после.

## Фазы

- **Сейчас (pre-launch):** ✅ Кирпич 2 (Отдых + честность) сделан, ✅ Кирпич 1 (захват рефлексии + недельное зеркало) сделан. Оба переживают отсутствие ключа. **Оба кирпича из плана готовы — цикл Захват→Понимание→Совет→Действие собран.**
- **✅ Джарвис-1 (26.07, по команде Альберта):** дыра «Совет советует вслепую» закрыта — чат-Помощник стал секретарём (видит состояние в моменте + неделю + рефлексию, отвечает адресно), предложения соизмеряют масштаб с нагрузкой, язык ответа = язык интерфейса. Это была вытяжка P2 вперёд — оправдана тем, что цикл уже собран, Джарвис теперь свойство, а не расфокус.
- **Джарвис-2 (кандидат, обсудить):** Тень как один голос вместо восьми нудж-карточек на «Сегодня» (локальный триггер выбирает важное, ИИ озвучивает, без ключа — статичный фолбэк) + карточки-действия прямо в чате. Заодно чинит «панель нечитаема» (fb #9).
- **P1 (после дизайн-системы):** «Заход» уже в проде (v1) — довести до ИИ-конструктора ритуала.
- **P2:** ИИ-секретарь по состоянию — проактивные подсказки («сегодня про восстановление, не про продуктивность»), связка со сном (`#13`), помощь с конкретной болью этапа (`#3`).
- **После стабильного D7:** «Джарвис» как надстройка над зрелым циклом; B2B — отдельный разговор.

## Что нужно от тебя
- Живой ИИ-прогон «Личной карты» вех (Фаза 3 дерева) — ключ только у тебя.
- Расшифровать «Лотерею мёда» (`#4`).
- ОК на парк B2B до запуска.
- Подтвердить порядок: сначала честное состояние (Отдых), потом ИИ-зеркало — или наоборот.
