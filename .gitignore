# Personal user data + session secret — never commit (stays on machine / server volume,
# recreated fresh on Railway/new instance at first run)
data/*.json
data/users/
data/feedback.json
data/feedback/
data/feedback-backups/

# macOS
.DS_Store
**/.DS_Store

# Node
node_modules/
npm-debug.log*
.env.local

# Runtime launcher (machine-specific)
Запустить.command
public/assets/shadow/gif-preview/
