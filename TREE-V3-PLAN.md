# Дерево навыков v3 — «Карта пути», а не магазин процентов

> 2026-07-17, по прямой команде Альберта («реализовано на 5% потенциала, контринтуитивная фигня, но очень полезная — плотно проработать»). Это ЧЕТВЁРТЫЙ репорт про дерево (`fb_mqe8clpol8dy`, `fb_mql49q336gl8` «нет даже концептуальной логики», `fb_mrnjz1qsjmk8`). v2-движок (8 перков, 11 шаблонов, капстоуны) остаётся — меняется концепция поверх него.

## Диагноз: почему 5%, а не 100

1. **Сломана метафора.** В любой RPG дерево отвечает на вопрос «кем я стану и что смогу делать». Наше отвечает «какой % бонуса выбрать». Ни один узел не меняет ни жизнь, ни игру — это бухгалтерия, притворяющаяся картой.
2. **Дерево не помнит реальность.** У бегуна и пловца деревья «Спорта» идентичны. Уровень 7 → 7 очков → любые узлы. Дерево ничего не говорит о том, кто ты в этой сфере.
3. **Выбор без последствий.** +2% XP против +3% золота — не выбор, а шум. Выбор работает, когда меняет поведение.
4. **Прогресс дерева оторван от прогресса жизни.** Узлы жмутся кнопкой. В сфере «Бег» человек интуитивно ждёт от «дерева навыков» лестницу: 5К → 10К → полумарафон. Мы показываем «+5% XP».

Пункт 4 — источник «контринтуитивности», которую ты назвал: интерфейс обещает карту мастерства, а выдаёт магазин бустов.

## Концепция: три слоя узлов

**🚩 Вехи (milestone)** — хребет дерева. Реальные достижения сферы из уже написанных лестниц мастерства (`IMPORT_LADDERS`: бег 5К→ультра, языки A1→C2, кодинг Junior→Lead, ~30 лестниц + generic). Открываются **не очками, а жизнью**: честное подтверждение «это правда взято» (философия честности Satoru — мы не полиция; позже — автоматом от числовых целей). Каждая взятая веха **даёт +1 очко практик** — жизнь финансирует бонусы, а не наоборот. Это ключевая сцепка слоёв.

**◈ Практики (perk)** — существующие v2-узлы за очки. Экономический слой: небольшие пассивки, теперь оплачиваемые в том числе вехами.

**👑 Капстоун** — есть, остаётся вершиной перк-слоя.

## Фазы

- **Фаза 1 (сейчас):** вехи в движке. Генерация хребта из `ladderFor(имя сферы)` в правой колонке дерева; для существующих деревьев — недеструктивный append (кастом не трогаем). `treePointsEarned = уровень + взятые вехи`. Клик по вехе → модалка честности → взято: звание-тост, очко, летопись-строка Тени. Фикс попутного бага: тост unlock-node показывал `undefined% XP` (legacy-поле).
- **Фаза 2:** связка вехи ↔ числовой цели (current→target уже есть): цель достигнута → веха предлагает себя сама. Синк с импортом достижений (указал тир N при импорте → вехи до N уже взяты). Тень комментирует веху в летописи.
- **Фаза 3:** ИИ-сборка персональной карты («Обсудить с Тенью»: из твоих целей и истории → твои вехи, не шаблонные). Редактор вех уже работает через существующий tree-editor.
- **Фаза 4:** визуал (после дизайн-системы Codex): вехи как флаги на тропе, пройденный путь подсвечен, «туман» впереди.

## Гейты

- Веха — приглашение, не долг: невзятые вехи не краснеют, не «просрочиваются», ничего не должны.
- Самоподтверждение без полиции: анти-чит не нужен, ты обманываешь только свою карту.
- Лестницы редактируемы: шаблон — старт, не приговор (в редакторе вехи = обычные узлы).
- ⏳ Долг Фазы 1: тексты лестниц (~30 × 6 тиров) — RU-only, перевод отдельным заходом (как SPHERE_GUIDE).
