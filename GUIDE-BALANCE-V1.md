# ГАЙД — Сферы + Колесо баланса (v1 «тест») + общий план

> **Самодостаточный гайд для чата-исполнителя (код).** Читать ТОЛЬКО: этот файл + `START-HERE.md` (тех-контекст и грабли) + те файлы Satoru, которые правишь. НЕ тащить сюда транскрипты обсуждений.
> Контекст философии (по желанию): `ALTERNEYT.md`. Ранний разбор `DD1-SPHERES.md` — **устарел** (писался на старых seed-данных), актуальная версия — этот файл.
> Дата: 2026-06-24.

---

## 1. Философия (НЕ нарушать)

1. **Цель — НЕ упростить систему, а сделать ПОЛЬЗОВАНИЕ сложной системой простым.** Сложность допустима и нужна (реалистичная жизнь сложна); перегруз в UX — нет. Строим «ручеёк», а не «слоёный пирог, который поехал в шоколадную кашу».
2. **Строим по одному слою.** На каждом шаге проверяем: «это всё ещё ручеёк?». **v1 = тест-скелет; реализм добавляем потом** (см. §6 Парковка), по одному, не сразу.
3. **Бренд:** жизнь как десятиборье · через любовь, не вину (никаких Duolingo-наказаний) · уровень/мастерство не сгорает (чёрный пояс) · отдых = труд. Детали — `ALTERNEYT.md`.

---

## 2. Универсальная логика сфер (одинакова у всех) + личный список

**Проблема:** у Оскара ~10 сфер, у Альберта 3, но они «более-менее похожи» — у Альберта одна сфера = то, что Оскар дробит на несколько. Логика распределения должна быть **ОДНА у всех**, чтобы никто не агонизировал над структурой.

**Решение — два уровня:**
- **Канонические жизненные домены — фикс, ~10, ОДИНАКОВЫ У ВСЕХ.** Это невидимый «хребет» + помощник на онбординге. Черновой список (финализировать, но он общий):
  `Тело/Здоровье · Отношения · Дело/Карьера · Деньги/Ресурсы · Развитие/Знания · Дух/Смысл · Творчество/Созидание · Отдых/Восстановление · Быт/Среда · Игра/Приключения`
  (Синтез Wheel of Life + десятиборья Хартмана.)
- **Личные сферы — список свой у каждого.** Пользователь называет и вкладывает как хочет; каждая **верхняя** сфера маппится на один канонический домен. Маппинг авто (LLM/эвристика по названию) + ручная правка. Пример Альберта: `Здоровье→Тело` · `Заработок→Дело` · `Учёба→Развитие`.

**Зачем:** (а) единая логика — структурные решения предустановлены; (б) онбординг видит дыры («нет домена Отношения — а у Хартмана это 40% десятиборья»); (в) фундамент мэтчинга в братстве (свожу по каноническому домену+навыку, а не по идиосинкразическим названиям).

**Важно для v1:** канонический слой = БЭКЕНД-логика + мягкая подсказка на онбординге. В основном UX юзер видит **свои** сферы. Не делать этот слой громоздким и не пихать в повседневный экран.

---

## 3. Колесо баланса = ритм внимания за период (ЯДРО v1)

- **Оси колеса = верхние сферы пользователя** (≤10). НЕ под-сферы (иначе «сорокаборье»).
- **Значение оси = ритм внимания за скользящее окно ~2–4 недели**: сколько активности ушло в сферу (задачи + привычки; время из `estimateMin`/`actualMin`, привязка по `skillId`). **НЕ стоячий уровень.**
- **Дрилл-даун:** клик по сфере → её под-сферы и ИХ ритм. Здесь виден **бег** (`Здоровье › Развитие тела › Run`): затих 3 недели → подсвечен «остыл». ← ответ на «как понять, проседает ли бег».
- **Баланс-скор + нудж:** показываем сферу с наибольшим разрывом → «подтяни X». В v1 разрыв считаем против простого таргета (равномерно ИЛИ ручной вес сферы). Режимы-цели — потом (§6).
- **Деградация в v1 = естественная:** затихшая сфера/под-сфера сама проседает на колесе, потому что окно её «не видит». **Отдельную математику деградации НЕ пишем.** Стартовый уровень-бирку НЕ трогаем — он остаётся храповиком («не сгорает»).

---

## 4. Что УЖЕ построено (НЕ переделывать — сверять с живым профилем на Railway)

Иерархия сфер N-уровней («Самостоятельная сфера» / «Внутри X»); импорт стартового уровня с лестницами (Ученик/Адепт/Эксперт/Мастер; контекстные: CEFR для языка, Abitur-ступень для школы; «Оценить через ИИ»); индекс баланса (оболочка «Баланс 54/100 · подтяни X»); радар-десятиборье; цели со статусами (active/maintaining/paused/done); задачи с временем; привычки; телосложение. 

→ v1 **дорабатывает** баланс (читать ритм + дрилл-даун) и **добавляет** канонический маппинг. НЕ строит сферы/импорт/радар заново.

> ⚠️ Локальные `data/users/albert/*.json` в репозитории — устаревший seed (level 1). Реальные данные — на проде. Сверять модель по проду/скринам, не по локальным файлам.

---

## 5. Скоуп v1 — строить СЕЙЧАС

1. **Колесо баланса читает ритм внимания за окно** (~2–4 нед), а не стоячие уровни.
2. **Дрилл-даун** в сферу → под-сферы + их ритм (бег виден, «остыл»-подсветка).
3. **Канонический маппинг** верхних сфер на ~10 доменов (авто + ручная правка) + **мягкая онбординг-подсказка** про пустые домены (особенно Отношения).
4. **Проекты — не сферы:** Satoru/JuFo не должны быть осями колеса. Подсказать перенос в Цели (они уже есть) либо просто не учитывать их как сферы в балансе.

**НЕ строить в v1:** мастерство-деградацию, типы узлов, режимы-цели, LLM-калибровку уровня, мэтчинг/братство. Всё это — §6.

---

## 6. Парковка — потом, по одному, в этом порядке (НЕ терять)

1. **Реализм/деградация:** двухтемповая — форма/острота тает за недели, мастерство/уровень подтекает за месяцы-годы простоя; пол «знаю как» (не до нуля); возврат быстрее первого раза (мышечная память); **без стрик-наказаний** — это зеркало реальности, не надзиратель. (ТЗ Альберта: реалистично, не мягко и не через наказание.)
2. **Типы узлов:** 🎯 навык / 🔄 поддержание / 🏁 проект — чтобы «Отдых и восстановление» не просил «уровень мастерства», а проект жил в Целях.
3. **Режимы-цели** (🔼 к вершине / ⚖️ держать / 🟰 не терять / ⏸️ пауза): баланс считается против **интенции**, а не равномерности (универсал-атлет vs «рвать в беге, зал поддерживать» — у обоих своя «норма»).
4. **Честный импорт уровня:** LLM-калибратор до листьев; уровень = качественная полоса (бег нельзя мерить дистанцией/временем), пара якорных вопросов → нормализация в полосу.
5. **Братство/мэтчинг:** по каноническому навыку + полосе уровня (LLM-семантика: «ММА» = «mixed martial arts»).

---

## 7. Acceptance — как понять, что v1 готов

- На колесе вижу свои верхние сферы; если 3 недели не трогал Здоровье — Здоровье просело и есть нудж «подтяни».
- Клик по Здоровью → вижу: Run затих, Gym активен (ритм под-сфер).
- Новая сфера авто-маппится на канонический домен; онбординг мягко говорит «нет домена Отношения».
- Проект (JuFo/Satoru) не засоряет колесо как ось.
- Ощущается ручейком, не перегрузом (главный критерий §1).

---

## 8. Тех-грабли (из START-HERE — обязательно соблюсти)

- **Кириллический `\b`** в JS-regex не матчит русские слова — никогда `\b` перед кириллицей.
- **Nav/FAB дублированы** в `index.html` И в `APP_SHELL`-const в `app.js` — править ОБА.
- **Превью:** после правок `app.js` — перезагрузить страницу; после `server.js` — `preview_stop`→`preview_start`.
- **Чистить тест-юзеров** после тестов; **`Store.save` дебаунсится** (250мс) — в тестах ждать.
- Деплой: `git push` → Railway авто-деплоит. Данные на персистентном томе `/app/data`.
