# Satoru — ТЗ для художницы (воксель-ассеты)

## Главное (ответ на «что вообще делать»)
- **Стиль:** воксель 3D, точно как комната, которую ты уже сделала. НЕ рисовать пикселями вручную. НЕ делать покадровую анимацию.
- **Инструмент на iPad:** **Voxel Max** (App Store, под iPad Pro + Apple Pencil; экспорт `.vox`). Альтернативы: Goxel (бесплатно), Mega Voxels. MagicaVoxel — только на Mac/PC (у Альберта на Mac для обработки). Все совместимы по `.vox` — пайплайн один.
- **Принцип:** каждая деталь — **отдельный файл**. Ты делаешь **кирпичики по одному раз**. Комбинации, перекраску, надевание гира, морф, сборку анимации — делает автоматизация (Альберт/код). Тебе НЕ нужно рисовать варианты комбинаций и цветов.
- **Что присылать на каждую деталь:** исходник `.vox` (главное!) + одна картинка-превью `.png` (рендер из MagicaVoxel), чтобы было видно как выглядит.

## Технические правила (одинаковые для ВСЕХ моделей)
1. **Один масштаб вокселя везде.** 1 кубик = один размер во всех моделях (персонаж, мебель, животное, гир) — не увеличивай/уменьшай кубики между моделями, иначе не стыкуется.
2. **Якорь снизу-по-центру.** Ставь каждую модель так, чтобы «низ» (ноги персонажа, дно мебели, лапы животного) был внизу сетки, по центру X/Z. Тогда всё встаёт на пол ровно.
3. **Лицом в одну сторону.** Все персонажи/животные «смотрят» в одну и ту же сторону (например, на камеру / в +X). Договоримся об одной — и держим везде.
4. **Одна общая палитра.** Сохрани одну палитру (как у комнаты — тёплая, классическая) и используй её во всех моделях. В MagicaVoxel можно сохранить/загрузить палитру — держим единую. Цветовые варианты НЕ делай (перекраска автоматическая).
5. **Нейтральный свет при экспорте превью** (без жёстких теней, запечённых в модель) — освещение комнаты по времени суток накладывает движок.
6. **Размеры-ориентиры:** персонаж ≈ 36–44 вокселя в высоту; животное ≈ 16–28; мебель — под комнату; гир (меч/шлем) — под руку/голову персонажа.

## Именование файлов (важно для автоматизации)
- Персонаж-база: `char_base_m.vox`, `char_base_f.vox`
- Причёски: `char_hair_01.vox`, `char_hair_02.vox`, …
- Одежда/наряды: `char_outfit_01.vox`, …
- Позы персонажа (для анимации, см. ниже): `pose_idle_1.vox`, `pose_idle_2.vox`, `pose_pushup_1.vox`, `pose_pushup_2.vox`, `pose_rest_1.vox`
- Комната: `room_base.vox`
- Декор (каждый предмет отдельно): `decor_window.vox`, `decor_plant.vox`, `decor_armchair.vox`, `decor_rug.vox`, `decor_fireplace.vox`, `decor_lamp.vox`, `decor_painting.vox`, …
- Животные: `pet_dragon_idle_1.vox`, `pet_dragon_idle_2.vox`, `pet_dragon_happy_1.vox`, `pet_panda_idle_1.vox`, …
- Гир/предметы: `item_sword_01.vox`, `item_helmet_01.vox`, `item_amulet_01.vox`, …

## Анимация — твой же подход «пара поз → гифка»
Анимацию НЕ рисуешь покадрово. На каждое действие делаешь **2 ключевые позы** (то же тело, разное положение) — Альберт склеит в зацикленную гифку.
- **Персонаж:** `idle` (стоит, лёгкое покачивание — 2 позы), `pushup` (отжимание: вверх/вниз — 2 позы), `rest` (сидит — 1–2 позы). *(меч/приставка/игра с питомцем — добавим позже теми же парами поз)*
- **Животное:** `idle` (2 позы), `happy` (2 позы), `sleep` (1 поза).
Каждая поза — отдельный `.vox` с тем же якорем (низ-по-центру), чтобы кадры не «прыгали».

## Пилотный батч (с этого начни — проверим всю петлю, потом масштабируем)
**Партия 1 — фундамент (минимум, чтобы заработало):**
1. `char_base_m.vox` — 1 базовый персонаж (без одежды/волос, нейтральный).
2. `char_hair_01.vox`, `char_hair_02.vox` — 2 причёски.
3. `char_outfit_01.vox`, `char_outfit_02.vox` — 2 наряда.
4. Позы базового персонажа: `pose_idle_1/2`, `pose_pushup_1/2`, `pose_rest_1`.
5. `room_base.vox` — пустая комната (стены+пол; у тебя уже есть — годится как старт).
6. Декор: `decor_window.vox`, `decor_plant.vox`, `decor_armchair.vox`, `decor_rug.vox` (4 предмета).
7. 1 животное: `pet_dragon_idle_1/2`, `pet_dragon_happy_1/2`, `pet_dragon_sleep_1`.
8. 2 предмета гира: `item_sword_01.vox`, `item_helmet_01.vox`.

Когда Партия 1 заработает в приложении — даём **Партию 2** (ещё причёски/наряды/декор/животные дракон→панда→собака и т.д.) и дальше масштабируем (часть вариаций добьём ИИ-генерацией в твоём стиле, ты курируешь).

## Что делает Альберт/автоматизация (НЕ ты)
- Рендер `.vox` → 2D-спрайты/гифки под нужным углом.
- Перекраска (палитра), надевание гира на персонажа/животное, морф частей по подсферам (мощные ноги и т.п.).
- Сборка комнаты из купленного декора, свет по времени суток.
- Подстановка нужной анимации по состоянию (тренировка → отжимания и т.д.).
- Сопоставление любой кастом-сферы юзера → подходящее животное-архетип.

## Доставка
Папки по типу, внутри — `.vox` + `.png`-превью:
```
/char/   (base, hair_*, outfit_*, pose_*)
/room/   (room_base, decor_*)
/pets/   (pet_*)
/items/  (item_*)
palette.png  (общая палитра)
```
Присылай батчами (сначала Партия 1). Если что-то непонятно по конкретной детали — спроси до того, как делать 10 штук.
